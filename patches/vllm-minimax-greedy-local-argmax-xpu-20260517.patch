diff --git a/vllm/benchmarks/throughput.py b/vllm/benchmarks/throughput.py
index 1c3b728fd..d2b3db283 100644
--- a/vllm/benchmarks/throughput.py
+++ b/vllm/benchmarks/throughput.py
@@ -44,6 +44,10 @@ from vllm.tokenizers import TokenizerLike, get_tokenizer
 from vllm.utils.async_utils import merge_async_iterators
 
 
+def benchmark_temperature() -> float:
+    return float(os.environ.get("VLLM_BENCH_TEMPERATURE", "1.0"))
+
+
 def run_vllm(
     requests: list[SampleRequest],
     n: int,
@@ -79,7 +83,7 @@ def run_vllm(
         sampling_params.append(
             SamplingParams(
                 n=n,
-                temperature=1.0,
+                temperature=benchmark_temperature(),
                 top_p=1.0,
                 ignore_eos=True,
                 max_tokens=request.expected_output_len,
@@ -159,7 +163,7 @@ def run_vllm_chat(
         sampling_params.append(
             SamplingParams(
                 n=n,
-                temperature=1.0,
+                temperature=benchmark_temperature(),
                 top_p=1.0,
                 ignore_eos=True,
                 max_tokens=request.expected_output_len,
@@ -219,7 +223,7 @@ async def run_vllm_async(
             sampling_params.append(
                 SamplingParams(
                     n=n,
-                    temperature=1.0,
+                    temperature=benchmark_temperature(),
                     top_p=1.0,
                     ignore_eos=True,
                     max_tokens=request.expected_output_len,
@@ -301,7 +305,7 @@ def run_hf(
             input_ids=input_ids.to(current_platform.device_type),
             do_sample=True,
             num_return_sequences=n,
-            temperature=1.0,
+            temperature=benchmark_temperature(),
             top_p=1.0,
             use_cache=True,
             max_new_tokens=max_output_len,
diff --git a/vllm/compilation/cuda_graph.py b/vllm/compilation/cuda_graph.py
index b63d86199..f86884229 100644
--- a/vllm/compilation/cuda_graph.py
+++ b/vllm/compilation/cuda_graph.py
@@ -2,6 +2,7 @@
 # SPDX-FileCopyrightText: Copyright contributors to the vLLM project
 
 import dataclasses
+import os
 import weakref
 from collections import Counter
 from collections.abc import Callable
@@ -129,6 +130,7 @@ class CUDAGraphEntry:
     batch_descriptor: BatchDescriptor
     cudagraph: torch.cuda.CUDAGraph | None = None
     output: Any | None = None
+    replay_count: int = 0
 
     # for cudagraph debugging, track the input addresses
     # during capture, and check if they are the same during replay
@@ -201,6 +203,10 @@ class CUDAGraphWrapper:
 
         if cudagraph_options is None:
             cudagraph_options = CUDAGraphOptions()
+        if os.environ.get("VLLM_XPU_CUDAGRAPH_STRONG_OUTPUT", "0") == "1":
+            cudagraph_options = dataclasses.replace(
+                cudagraph_options, weak_ref_output=False
+            )
         self.cudagraph_options = cudagraph_options
         # the entries for different batch descriptors that we need to capture
         # cudagraphs for.
@@ -230,6 +236,14 @@ class CUDAGraphWrapper:
     def clear_graphs(self) -> None:
         self.concrete_cudagraph_entries.clear()
 
+    @staticmethod
+    def _recapture_after_n_replays() -> int:
+        raw = os.environ.get("VLLM_XPU_CUDAGRAPH_RECAPTURE_AFTER_N_REPLAYS", "0")
+        try:
+            return max(0, int(raw))
+        except ValueError:
+            return 0
+
     def __call__(self, *args: Any, **kwargs: Any) -> Any | None:
         if not is_forward_context_available():
             # No forward context means we are outside the normal
@@ -357,5 +371,18 @@ class CUDAGraphWrapper:
         # Sync offloader before replay - ensures any external dependencies
         # from pre-capture prefetches are satisfied.
         get_offloader().sync_prev_onload()
+        recapture_after = self._recapture_after_n_replays()
+        if recapture_after and entry.replay_count >= recapture_after:
+            entry.cudagraph = None
+            entry.output = None
+            entry.input_addresses = None
+            entry.replay_count = 0
+            return self.__call__(*args, **kwargs)
+        sync_replay = os.environ.get("VLLM_XPU_SYNC_CUDAGRAPH_REPLAY", "0") == "1"
+        if sync_replay and hasattr(torch, "xpu"):
+            torch.xpu.synchronize()
         entry.cudagraph.replay()
+        entry.replay_count += 1
+        if sync_replay and hasattr(torch, "xpu"):
+            torch.xpu.synchronize()
         return entry.output
diff --git a/vllm/distributed/device_communicators/base_device_communicator.py b/vllm/distributed/device_communicators/base_device_communicator.py
index 0b4b81f93..29c5a3084 100644
--- a/vllm/distributed/device_communicators/base_device_communicator.py
+++ b/vllm/distributed/device_communicators/base_device_communicator.py
@@ -1,5 +1,6 @@
 # SPDX-License-Identifier: Apache-2.0
 # SPDX-FileCopyrightText: Copyright contributors to the vLLM project
+import os
 import threading
 from weakref import WeakValueDictionary
 
@@ -186,18 +187,42 @@ class DeviceCommunicatorBase:
             # Convert negative dim to positive.
             dim += input_.dim()
         input_size = input_.size()
-        # NOTE: we have to use concat-style all-gather here,
-        # stack-style all-gather has compatibility issues with
-        # torch.compile . see https://github.com/pytorch/pytorch/issues/138795
-        output_size = (input_size[0] * self.world_size,) + input_size[1:]
-        # Allocate output tensor.
-        output_tensor = torch.empty(
-            output_size, dtype=input_.dtype, device=input_.device
+        input_for_gather = (
+            input_.contiguous()
+            if os.environ.get("VLLM_XPU_CONTIGUOUS_ALL_GATHER", "0") == "1"
+            else input_
         )
-        # All-gather.
-        dist.all_gather_into_tensor(output_tensor, input_, group=self.device_group)
+        if (
+            os.environ.get("VLLM_XPU_SYNC_ALL_GATHER", "0") == "1"
+            and input_.device.type == "xpu"
+        ):
+            torch.xpu.synchronize()
+        if os.environ.get("VLLM_XPU_LIST_ALL_GATHER", "0") == "1":
+            gather_list = [
+                torch.empty_like(input_for_gather) for _ in range(self.world_size)
+            ]
+            dist.all_gather(gather_list, input_for_gather, group=self.device_group)
+            output_tensor = torch.stack(gather_list, dim=0)
+        else:
+            # NOTE: we have to use concat-style all-gather here,
+            # stack-style all-gather has compatibility issues with
+            # torch.compile . see https://github.com/pytorch/pytorch/issues/138795
+            output_size = (input_size[0] * self.world_size,) + input_size[1:]
+            # Allocate output tensor.
+            output_tensor = torch.empty(
+                output_size, dtype=input_.dtype, device=input_.device
+            )
+            # All-gather.
+            dist.all_gather_into_tensor(
+                output_tensor, input_for_gather, group=self.device_group
+            )
+            output_tensor = output_tensor.reshape((self.world_size,) + input_size)
+        if (
+            os.environ.get("VLLM_XPU_SYNC_ALL_GATHER", "0") == "1"
+            and input_.device.type == "xpu"
+        ):
+            torch.xpu.synchronize()
         # Reshape
-        output_tensor = output_tensor.reshape((self.world_size,) + input_size)
         output_tensor = output_tensor.movedim(0, dim)
         output_tensor = output_tensor.reshape(
             input_size[:dim]
diff --git a/vllm/model_executor/layers/logits_processor.py b/vllm/model_executor/layers/logits_processor.py
index 3541b9706..c701da66f 100644
--- a/vllm/model_executor/layers/logits_processor.py
+++ b/vllm/model_executor/layers/logits_processor.py
@@ -2,16 +2,20 @@
 # SPDX-FileCopyrightText: Copyright contributors to the vLLM project
 """A layer that compute logits from hidden_stats."""
 
+import os
+
 import torch
 
 from vllm.distributed import (
     get_tensor_model_parallel_world_size,
+    get_tp_group,
     tensor_model_parallel_all_gather,
     tensor_model_parallel_gather,
 )
 from vllm.model_executor.custom_op import PluggableLayer
 from vllm.model_executor.layers.vocab_parallel_embedding import VocabParallelEmbedding
 from vllm.platforms import current_platform
+from vllm.utils.xpu_finite_trace import trace_tensor
 
 
 # --8<-- [start:logits_processor]
@@ -74,6 +78,12 @@ class LogitsProcessor(PluggableLayer):
 
     def _gather_logits(self, logits: torch.Tensor) -> torch.Tensor:
         """gather/all-gather the logits tensor across model parallel group."""
+        if (
+            os.environ.get("VLLM_XPU_GATHER_LOGITS_FP32", "0") == "1"
+            and logits.device.type == "xpu"
+            and logits.dtype != torch.float32
+        ):
+            logits = logits.float()
         if self.use_all_gather:
             # Gather is not supported for some devices such as TPUs.
             # Use all-gather instead.
@@ -93,14 +103,18 @@ class LogitsProcessor(PluggableLayer):
         embedding_bias: torch.Tensor | None,
     ) -> torch.Tensor | None:
         # Get the logits for the next tokens.
+        trace_tensor("logits_processor.hidden_states", hidden_states)
         logits = lm_head.quant_method.apply(lm_head, hidden_states, bias=embedding_bias)
+        trace_tensor("logits_processor.local_logits", logits)
 
         # Gather logits for TP
         logits = self._gather_logits(logits)
+        trace_tensor("logits_processor.gathered_logits", logits)
 
         # Remove paddings in vocab (if any).
         if logits is not None:
             logits = logits[..., : self.org_vocab_size]
+            trace_tensor("logits_processor.trimmed_logits", logits)
         return logits
 
     def get_top_tokens(
@@ -123,6 +137,7 @@ class LogitsProcessor(PluggableLayer):
         tp_size = get_tensor_model_parallel_world_size()
 
         logits = lm_head.quant_method.apply(lm_head, hidden_states, bias=embedding_bias)
+        trace_tensor("logits_processor.local_argmax_logits", logits)
         if self.soft_cap is not None:
             logits = torch.tanh(logits / self.soft_cap) * self.soft_cap
         if self.scale != 1.0:
@@ -147,13 +162,47 @@ class LogitsProcessor(PluggableLayer):
         local_pair = torch.stack(
             [local_max_vals.float(), global_indices.float()], dim=-1
         )
+        trace_tensor("logits_processor.local_argmax_pair", local_pair)
+        if os.environ.get("VLLM_XPU_LOCAL_ARGMAX_CPU_GATHER", "0") == "1":
+            if local_pair.device.type == "xpu":
+                torch.xpu.synchronize()
+            local_vals = local_max_vals.detach().float().cpu().tolist()
+            local_indices = global_indices.detach().to(torch.int64).cpu().tolist()
+            local_obj = [
+                (float(value), int(index))
+                for value, index in zip(local_vals, local_indices, strict=True)
+            ]
+            gathered_obj: list[list[tuple[float, int]] | None] = [None] * tp_size
+            torch.distributed.all_gather_object(
+                gathered_obj, local_obj, group=get_tp_group().cpu_group
+            )
+            top_tokens: list[int] = []
+            for batch_idx in range(hidden_states.shape[0]):
+                best_value = None
+                best_token = 0
+                for rank_obj in gathered_obj:
+                    if rank_obj is None:
+                        continue
+                    value, token = rank_obj[batch_idx]
+                    if best_value is None or value > best_value:
+                        best_value = value
+                        best_token = token
+                top_tokens.append(best_token)
+            result = torch.tensor(
+                top_tokens, dtype=torch.int64, device=hidden_states.device
+            )
+            trace_tensor("logits_processor.local_argmax_cpu_top_tokens", result)
+            return result
         # [batch, 2] -> [batch, 2 * tp_size]
         gathered = tensor_model_parallel_all_gather(local_pair, dim=-1)
+        trace_tensor("logits_processor.local_argmax_gathered", gathered)
         # [batch, tp_size, 2] where [:, :, 0]=values, [:, :, 1]=indices
         gathered = gathered.view(hidden_states.shape[0], tp_size, 2)
         max_rank_idx = gathered[:, :, 0].argmax(dim=-1, keepdim=True)
         top_tokens = gathered[:, :, 1].gather(dim=-1, index=max_rank_idx)
-        return top_tokens.squeeze(-1).to(torch.int64)
+        top_tokens = top_tokens.squeeze(-1).to(torch.int64)
+        trace_tensor("logits_processor.local_argmax_top_tokens", top_tokens)
+        return top_tokens
 
     def extra_repr(self) -> str:
         s = f"vocab_size={self.vocab_size}"
diff --git a/vllm/model_executor/models/minimax_m2.py b/vllm/model_executor/models/minimax_m2.py
index 818b7e681..cd3696eee 100644
--- a/vllm/model_executor/models/minimax_m2.py
+++ b/vllm/model_executor/models/minimax_m2.py
@@ -23,6 +23,7 @@
 # limitations under the License.
 """Inference-only MiniMaxM2 model."""
 
+import os
 from collections.abc import Iterable
 from itertools import islice
 from typing import Any
@@ -33,6 +34,7 @@ from transformers import PretrainedConfig
 
 from vllm.compilation.decorators import support_torch_compile
 from vllm.config import CacheConfig, ModelConfig, VllmConfig
+from vllm.distributed.communication_op import tensor_model_parallel_all_reduce
 from vllm.distributed import (
     get_pp_group,
     get_tensor_model_parallel_rank,
@@ -62,6 +64,8 @@ from vllm.model_executor.model_loader.weight_utils import (
     maybe_remap_kv_scale_name,
 )
 from vllm.sequence import IntermediateTensors
+from vllm.utils.xpu_decode_timing import timed_region
+from vllm.utils.xpu_finite_trace import trace_tensor_graph
 
 from .interfaces import EagleModelMixin, SupportsEagle3, SupportsLoRA, SupportsPP
 from .utils import (
@@ -74,6 +78,171 @@ from .utils import (
 )
 
 
+_MINIMAX_M2_FP16_ROUTER = (
+    os.environ.get("VLLM_MINIMAX_M2_FP16_ROUTER", "0") == "1"
+)
+_MINIMAX_M2_FP16_ROUTER_AUDIT = (
+    os.environ.get("VLLM_MINIMAX_M2_FP16_ROUTER_AUDIT", "0") == "1"
+)
+_MINIMAX_M2_CANDIDATE_ROUTER_TOPM = int(
+    os.environ.get("VLLM_MINIMAX_M2_CANDIDATE_ROUTER_TOPM", "0")
+)
+_MINIMAX_M2_CANDIDATE_ROUTER_MAX_TOKENS = int(
+    os.environ.get("VLLM_MINIMAX_M2_CANDIDATE_ROUTER_MAX_TOKENS", "4")
+)
+_MINIMAX_M2_CANDIDATE_ROUTER_XPU_REPAIR = (
+    os.environ.get("VLLM_MINIMAX_M2_CANDIDATE_ROUTER_XPU_REPAIR", "0") == "1"
+)
+_MINIMAX_M2_ATTN_DELAY_ALLREDUCE = (
+    os.environ.get("VLLM_MINIMAX_M2_ATTN_DELAY_ALLREDUCE", "0") == "1"
+)
+_MINIMAX_M2_MOE_DELAY_ALLREDUCE = (
+    os.environ.get("VLLM_MINIMAX_MOE_DELAY_ALLREDUCE", "0") == "1"
+)
+_MINIMAX_M2_DIST_RESIDUAL_ALLREDUCE = (
+    os.environ.get("VLLM_MINIMAX_M2_DIST_RESIDUAL_ALLREDUCE", "0") == "1"
+)
+_MINIMAX_QK_RMS_XPU_HELPER = (
+    os.environ.get("VLLM_MINIMAX_QK_RMS_XPU_HELPER", "0") == "1"
+)
+_MINIMAX_QK_RMS_XPU_HELPER_MAX_TOKENS = int(
+    os.environ.get("VLLM_MINIMAX_QK_RMS_XPU_HELPER_MAX_TOKENS", "4")
+)
+_MINIMAX_QK_NORM_EAGER = (
+    os.environ.get("VLLM_MINIMAX_QK_NORM_EAGER", "0") == "1"
+)
+_minimax_qk_rms_xpu = None
+if _MINIMAX_QK_RMS_XPU_HELPER:
+    try:
+        import minimax_qk_rms_xpu as _minimax_qk_rms_xpu
+
+        try:
+            torch.library.register_fake("minimax_qk_rms_xpu::var")(
+                lambda qkv, qk_var, q_size, kv_size: None
+            )
+            torch.library.register_fake("minimax_qk_rms_xpu::var_return")(
+                lambda qkv, qk_var, q_size, kv_size: qk_var
+            )
+            torch.library.register_fake("minimax_qk_rms_xpu::var_alloc")(
+                lambda qkv, q_size, kv_size: qkv.new_empty(
+                    (qkv.shape[0], 2), dtype=torch.float32
+                )
+            )
+            torch.library.register_fake("minimax_qk_rms_xpu::apply")(
+                lambda qkv,
+                qk_var,
+                q_weight,
+                k_weight,
+                q_out,
+                k_out,
+                q_size,
+                kv_size,
+                eps: None
+            )
+            torch.library.register_fake("minimax_qk_rms_xpu::apply_return")(
+                lambda qkv,
+                qk_var,
+                q_weight,
+                k_weight,
+                q_out,
+                k_out,
+                q_size,
+                kv_size,
+                eps: (q_out, k_out)
+            )
+            torch.library.register_fake("minimax_qk_rms_xpu::apply_alloc")(
+                lambda qkv,
+                qk_var,
+                q_weight,
+                k_weight,
+                q_size,
+                kv_size,
+                eps: (
+                    qkv.new_empty((qkv.shape[0], q_size)),
+                    qkv.new_empty((qkv.shape[0], kv_size)),
+                )
+            )
+            torch.library.register_fake("minimax_qk_rms_xpu::apply_f32_weight")(
+                lambda qkv,
+                qk_var,
+                q_weight,
+                k_weight,
+                q_out,
+                k_out,
+                q_size,
+                kv_size,
+                eps: None
+            )
+            torch.library.register_fake(
+                "minimax_qk_rms_xpu::apply_f32_weight_return"
+            )(
+                lambda qkv,
+                qk_var,
+                q_weight,
+                k_weight,
+                q_out,
+                k_out,
+                q_size,
+                kv_size,
+                eps: (q_out, k_out)
+            )
+            torch.library.register_fake(
+                "minimax_qk_rms_xpu::apply_f32_weight_alloc"
+            )(
+                lambda qkv,
+                qk_var,
+                q_weight,
+                k_weight,
+                q_size,
+                kv_size,
+                eps: (
+                    qkv.new_empty((qkv.shape[0], q_size)),
+                    qkv.new_empty((qkv.shape[0], kv_size)),
+                )
+            )
+        except RuntimeError:
+            pass
+    except Exception:
+        _minimax_qk_rms_xpu = None
+
+_minimax_m2_candidate_repair_topk = None
+_minimax_m2_candidate_repair_import_failed = False
+
+
+@torch.compiler.disable
+def _minimax_forward_qk_eager(
+    q_norm: MiniMaxText01RMSNormTP,
+    k_norm: MiniMaxText01RMSNormTP,
+    q: torch.Tensor,
+    k: torch.Tensor,
+) -> tuple[torch.Tensor, torch.Tensor]:
+    return MiniMaxText01RMSNormTP.forward_qk(q_norm, k_norm, q, k)
+
+
+def _get_minimax_m2_candidate_repair_topk():
+    global _minimax_m2_candidate_repair_topk
+    global _minimax_m2_candidate_repair_import_failed
+    if (
+        not _MINIMAX_M2_CANDIDATE_ROUTER_XPU_REPAIR
+        or _minimax_m2_candidate_repair_import_failed
+    ):
+        return None
+    if _minimax_m2_candidate_repair_topk is not None:
+        return _minimax_m2_candidate_repair_topk
+    try:
+        from custom_esimd_kernels_vllm import (
+            minimax_m2_candidate_repair_topk as repair_topk,
+        )
+        if not hasattr(torch.ops.moe_int4_ops, "minimax_m2_candidate_repair_topk"):
+            _minimax_m2_candidate_repair_import_failed = True
+            return None
+    except Exception:
+        _minimax_m2_candidate_repair_import_failed = True
+        return None
+    _minimax_m2_candidate_repair_topk = repair_topk
+    return repair_topk
+
+
 class MiniMaxM2MoE(nn.Module):
     def __init__(
         self,
@@ -83,6 +252,8 @@ class MiniMaxM2MoE(nn.Module):
     ):
         super().__init__()
         self.tp_size = get_tensor_model_parallel_world_size()
+        self.layer_name = prefix
+        self._fp16_router_audit_printed: set[str] = set()
 
         if self.tp_size > config.num_local_experts:
             raise ValueError(
@@ -127,15 +298,210 @@ class MiniMaxM2MoE(nn.Module):
         assert param.size() == loaded_weight.size()
         param.data.copy_(loaded_weight.to(torch.float32))
 
+    def _audit_fp16_router(
+        self,
+        router_logits_fp32: torch.Tensor,
+        router_logits_fp16: torch.Tensor,
+        num_tokens: int,
+    ) -> None:
+        audit_key = "decode" if num_tokens == 1 else "prefill"
+        if audit_key in self._fp16_router_audit_printed:
+            return
+        self._fp16_router_audit_printed.add(audit_key)
+
+        top_k = int(getattr(self.experts, "top_k", 8))
+        with torch.no_grad():
+            fp32_choice_scores = self._router_choice_scores(router_logits_fp32)
+            fp16_choice_scores = self._router_choice_scores(router_logits_fp16)
+            _, fp32_idx = torch.topk(fp32_choice_scores, k=top_k, dim=-1)
+            _, fp16_idx = torch.topk(fp16_choice_scores, k=top_k, dim=-1)
+            ordered_mismatch = (fp32_idx != fp16_idx).any(dim=-1).sum().item()
+            set_mismatch = (
+                torch.sort(fp32_idx, dim=-1).values
+                != torch.sort(fp16_idx, dim=-1).values
+            ).any(dim=-1).sum().item()
+            max_abs_logit_delta = (
+                router_logits_fp32 - router_logits_fp16
+            ).abs().max().item()
+            candidate_misses: list[tuple[int, int]] = []
+            max_candidates = router_logits_fp16.shape[-1]
+            for candidate_count in (12, 16, 32):
+                if candidate_count > max_candidates:
+                    continue
+                _, fp16_candidates = torch.topk(
+                    fp16_choice_scores, k=candidate_count, dim=-1
+                )
+                covered = (
+                    fp32_idx.unsqueeze(-1) == fp16_candidates.unsqueeze(-2)
+                ).any(dim=-1).all(dim=-1)
+                candidate_misses.append(
+                    (candidate_count, (~covered).sum().item())
+                )
+            max_abs_choice_delta = (
+                fp32_choice_scores - fp16_choice_scores
+            ).abs().max().item()
+
+        print(
+            "[minimax-fp16-router-audit] "
+            f"layer={self.layer_name} phase={audit_key} tokens={num_tokens} "
+            f"top_k={top_k} ordered_mismatch={ordered_mismatch} "
+            f"set_mismatch={set_mismatch} "
+            f"max_abs_logit_delta={max_abs_logit_delta:.6g} "
+            f"max_abs_choice_delta={max_abs_choice_delta:.6g} "
+            + " ".join(
+                f"top{candidate_count}_candidate_miss={miss_count}"
+                for candidate_count, miss_count in candidate_misses
+            ),
+            flush=True,
+        )
+
+    def _router_choice_scores(self, router_logits: torch.Tensor) -> torch.Tensor:
+        scoring_func = getattr(self.experts, "scoring_func", "softmax")
+        if scoring_func == "sigmoid":
+            scores = router_logits.sigmoid()
+        elif scoring_func == "softmax":
+            scores = router_logits.softmax(dim=-1)
+        elif scoring_func == "sqrtsoftplus":
+            scores = torch.nn.functional.softplus(router_logits).sqrt()
+        else:
+            scores = router_logits
+        if self.e_score_correction_bias is not None:
+            scores = scores + self.e_score_correction_bias.to(
+                scores.dtype
+            ).unsqueeze(0)
+        return scores
+
+    def _candidate_router_topk(
+        self, hidden_states: torch.Tensor, candidate_count: int
+    ) -> tuple[torch.Tensor, torch.Tensor, torch.Tensor]:
+        top_k = int(getattr(self.experts, "top_k", 8))
+        candidate_count = max(top_k, candidate_count)
+        gate_weight_fp32 = getattr(self.gate, "weight", None)
+        if gate_weight_fp32 is None:
+            raise RuntimeError("MiniMax candidate router requires gate.weight")
+        if getattr(self.experts, "scoring_func", "softmax") != "sigmoid":
+            raise RuntimeError("MiniMax candidate router currently expects sigmoid routing")
+        if self.e_score_correction_bias is None:
+            raise RuntimeError("MiniMax candidate router expects correction bias")
+
+        router_logits_fp16 = torch.nn.functional.linear(
+            hidden_states.to(torch.float16), self._gate_weight_fp16
+        ).to(torch.float32)
+        choice_scores_fp16 = self._router_choice_scores(router_logits_fp16)
+        _, candidate_ids = torch.topk(
+            choice_scores_fp16, k=candidate_count, dim=-1
+        )
+
+        repair_topk = _get_minimax_m2_candidate_repair_topk()
+        if repair_topk is not None and hidden_states.device.type == "xpu":
+            topk_weights, topk_ids = repair_topk(
+                hidden_states,
+                candidate_ids,
+                gate_weight_fp32,
+                self.e_score_correction_bias,
+                top_k,
+                getattr(self.experts, "renormalize", True),
+            )
+            return topk_weights.contiguous(), topk_ids.contiguous(), router_logits_fp16
+
+        candidate_weights = gate_weight_fp32[candidate_ids.long()]
+        exact_logits = (
+            candidate_weights * hidden_states.to(torch.float32).unsqueeze(1)
+        ).sum(dim=-1)
+        exact_scores = exact_logits.sigmoid()
+        exact_choice_scores = exact_scores + self.e_score_correction_bias[
+            candidate_ids.long()
+        ].to(exact_scores.dtype)
+        _, local_topk = torch.topk(exact_choice_scores, k=top_k, dim=-1)
+        topk_ids = candidate_ids.gather(1, local_topk).to(torch.int32)
+        topk_weights = exact_scores.gather(1, local_topk).to(torch.float32)
+        if getattr(self.experts, "renormalize", True):
+            topk_weights = topk_weights / topk_weights.sum(dim=-1, keepdim=True)
+        return topk_weights.contiguous(), topk_ids.contiguous(), router_logits_fp16
+
+    def _can_use_candidate_router(
+        self, hidden_states: torch.Tensor, num_tokens: int
+    ) -> bool:
+        if (
+            _MINIMAX_M2_CANDIDATE_ROUTER_TOPM <= 0
+            or not hasattr(self, "_gate_weight_fp16")
+            or num_tokens > _MINIMAX_M2_CANDIDATE_ROUTER_MAX_TOKENS
+        ):
+            return False
+        runner = self.experts.runner
+        if getattr(runner, "do_naive_dispatch_combine", False):
+            return False
+        if getattr(runner.moe_config, "pcp_size", 1) != 1:
+            return False
+        quant_method = getattr(runner, "_quant_method", None)
+        if quant_method is None or getattr(quant_method, "is_monolithic", False):
+            return False
+        should_apply_decode_monolithic = getattr(
+            quant_method, "should_apply_decode_monolithic", None
+        )
+        if (
+            should_apply_decode_monolithic is not None
+            and should_apply_decode_monolithic(self.experts, hidden_states)
+        ):
+            return False
+        return True
+
     def forward(self, hidden_states: torch.Tensor) -> torch.Tensor:
         num_tokens, hidden_dim = hidden_states.shape
         hidden_states = hidden_states.view(-1, hidden_dim)
 
+        if self._can_use_candidate_router(hidden_states, num_tokens):
+            with timed_region("minimax.moe.router_linear"):
+                topk_weights, topk_ids, router_logits = self._candidate_router_topk(
+                    hidden_states, _MINIMAX_M2_CANDIDATE_ROUTER_TOPM
+                )
+                if _MINIMAX_M2_FP16_ROUTER_AUDIT:
+                    router_logits_fp32, _ = self.gate(hidden_states.to(torch.float32))
+                    self._audit_fp16_router(
+                        router_logits_fp32, router_logits, num_tokens
+                    )
+                self.experts.router._precomputed_topk = (topk_weights, topk_ids)
+            with timed_region("minimax.moe.experts_total"):
+                try:
+                    final_hidden_states = self.experts(
+                        hidden_states=hidden_states, router_logits=router_logits
+                    )
+                finally:
+                    if (
+                        getattr(self.experts.router, "_precomputed_topk", None)
+                        is not None
+                    ):
+                        self.experts.router._precomputed_topk = None
+            return final_hidden_states.view(num_tokens, hidden_dim)
+
         # router_logits: (num_tokens, n_experts)
-        router_logits, _ = self.gate(hidden_states.to(torch.float32))
-        final_hidden_states = self.experts(
-            hidden_states=hidden_states, router_logits=router_logits
-        )
+        with timed_region("minimax.moe.router_linear"):
+            if _MINIMAX_M2_FP16_ROUTER and hasattr(self, "_gate_weight_fp16"):
+                router_logits_fp16 = torch.nn.functional.linear(
+                    hidden_states.to(torch.float16), self._gate_weight_fp16
+                ).to(torch.float32)
+                if _MINIMAX_M2_FP16_ROUTER_AUDIT:
+                    router_logits_fp32, _ = self.gate(hidden_states.to(torch.float32))
+                    self._audit_fp16_router(
+                        router_logits_fp32, router_logits_fp16, num_tokens
+                    )
+                router_logits = router_logits_fp16
+            else:
+                router_logits, _ = self.gate(hidden_states.to(torch.float32))
+                if (
+                    _MINIMAX_M2_FP16_ROUTER_AUDIT
+                    and hasattr(self, "_gate_weight_fp16")
+                ):
+                    router_logits_fp16 = torch.nn.functional.linear(
+                        hidden_states.to(torch.float16), self._gate_weight_fp16
+                    ).to(torch.float32)
+                    self._audit_fp16_router(
+                        router_logits, router_logits_fp16, num_tokens
+                    )
+        with timed_region("minimax.moe.experts_total"):
+            final_hidden_states = self.experts(
+                hidden_states=hidden_states, router_logits=router_logits
+            )
 
         return final_hidden_states.view(num_tokens, hidden_dim)
 
@@ -158,6 +524,7 @@ class MiniMaxM2Attention(nn.Module):
         prefix: str = "",
     ) -> None:
         super().__init__()
+        self.layer_name = prefix
         self.hidden_size = hidden_size
         tp_size = get_tensor_model_parallel_world_size()
         self.total_num_heads = num_heads
@@ -195,6 +562,7 @@ class MiniMaxM2Attention(nn.Module):
             bias=False,
             quant_config=quant_config,
             prefix=f"{prefix}.o_proj",
+            reduce_results=not _MINIMAX_M2_ATTN_DELAY_ALLREDUCE,
         )
 
         if (
@@ -221,33 +589,106 @@ class MiniMaxM2Attention(nn.Module):
         self.q_norm = MiniMaxText01RMSNormTP(
             self.head_dim * self.total_num_heads, eps=rms_norm_eps
         )
-        if self.total_num_kv_heads >= tp_size:
-            self.k_norm = MiniMaxText01RMSNormTP(
-                self.head_dim * self.total_num_kv_heads, eps=rms_norm_eps
-            )
-        else:
-            # KV heads are replicated across TP ranks; shard k_norm weight by
-            # total_num_kv_heads rather than tp_size to avoid incorrect sharding.
-            num_kv_head_replicas = tp_size // self.total_num_kv_heads
-            self.k_norm = MiniMaxText01RMSNormTP(
-                self.head_dim * self.total_num_kv_heads,
-                eps=rms_norm_eps,
-                weight_shard_world_size=self.total_num_kv_heads,
-                weight_shard_rank=get_tensor_model_parallel_rank()
-                // num_kv_head_replicas,
-            )
+        self.q_norm.trace_name = f"{prefix}.q_norm"
+        self.k_norm = MiniMaxText01RMSNormTP(
+            self.head_dim * self.total_num_kv_heads, eps=rms_norm_eps
+        )
+        self.k_norm.trace_name = f"{prefix}.k_norm"
 
     def forward(
         self,
         positions: torch.Tensor,
         hidden_states: torch.Tensor,
     ) -> torch.Tensor:
-        qkv, _ = self.qkv_proj(hidden_states)
-        q, k, v = qkv.split([self.q_size, self.kv_size, self.kv_size], dim=-1)
-        q, k = MiniMaxText01RMSNormTP.forward_qk(self.q_norm, self.k_norm, q, k)
-        q, k = self.rotary_emb(positions, q, k)
-        attn_output = self.attn(q, k, v)
-        output, _ = self.o_proj(attn_output)
+        with timed_region("minimax.attn.qkv"):
+            qkv, _ = self.qkv_proj(hidden_states)
+        trace_tensor_graph(f"{self.layer_name}.qkv", qkv)
+        use_qk_helper = (
+            _MINIMAX_QK_RMS_XPU_HELPER
+            and qkv.shape[0] <= _MINIMAX_QK_RMS_XPU_HELPER_MAX_TOKENS
+            and qkv.is_xpu
+            and qkv.is_contiguous()
+            and qkv.dtype in (torch.float16, torch.bfloat16)
+            and _minimax_qk_rms_xpu is not None
+            and hasattr(torch.ops, "minimax_qk_rms_xpu")
+            and hasattr(torch.ops.minimax_qk_rms_xpu, "var_alloc")
+            and (
+                (
+                    self.q_norm.weight.dtype == qkv.dtype
+                    and self.k_norm.weight.dtype == qkv.dtype
+                    and hasattr(torch.ops.minimax_qk_rms_xpu, "apply_alloc")
+                )
+                or (
+                    self.q_norm.weight.dtype == torch.float32
+                    and self.k_norm.weight.dtype == torch.float32
+                    and hasattr(
+                        torch.ops.minimax_qk_rms_xpu,
+                        "apply_f32_weight_alloc",
+                    )
+                )
+            )
+        )
+        if use_qk_helper:
+            with timed_region("minimax.attn.qk_norm"):
+                qk_var = torch.ops.minimax_qk_rms_xpu.var_alloc(
+                    qkv, self.q_size, self.kv_size
+                )
+                if self.q_norm.tp_world > 1:
+                    qk_var = (
+                        tensor_model_parallel_all_reduce(qk_var)
+                        / self.q_norm.tp_world
+                    )
+                q_weight = MiniMaxText01RMSNormTP._guard_weight(
+                    self.q_norm, f"{self.layer_name}.q_norm", qkv.shape[0]
+                )
+                k_weight = MiniMaxText01RMSNormTP._guard_weight(
+                    self.k_norm, f"{self.layer_name}.k_norm", qkv.shape[0]
+                )
+                if q_weight.dtype == qkv.dtype and k_weight.dtype == qkv.dtype:
+                    q, k = torch.ops.minimax_qk_rms_xpu.apply_alloc(
+                        qkv,
+                        qk_var,
+                        q_weight,
+                        k_weight,
+                        self.q_size,
+                        self.kv_size,
+                        self.q_norm.variance_epsilon,
+                    )
+                else:
+                    q, k = torch.ops.minimax_qk_rms_xpu.apply_f32_weight_alloc(
+                        qkv,
+                        qk_var,
+                        q_weight,
+                        k_weight,
+                        self.q_size,
+                        self.kv_size,
+                        self.q_norm.variance_epsilon,
+                    )
+            v = qkv.split([self.q_size, self.kv_size, self.kv_size], dim=-1)[2]
+        else:
+            q, k, v = qkv.split([self.q_size, self.kv_size, self.kv_size], dim=-1)
+            with timed_region("minimax.attn.qk_norm"):
+                if _MINIMAX_QK_NORM_EAGER:
+                    q, k = _minimax_forward_qk_eager(
+                        self.q_norm, self.k_norm, q, k
+                    )
+                else:
+                    q, k = MiniMaxText01RMSNormTP.forward_qk(
+                        self.q_norm, self.k_norm, q, k
+                    )
+        trace_tensor_graph(f"{self.layer_name}.q_after_qk_norm", q)
+        trace_tensor_graph(f"{self.layer_name}.k_after_qk_norm", k)
+        trace_tensor_graph(f"{self.layer_name}.v", v)
+        with timed_region("minimax.attn.rope"):
+            q, k = self.rotary_emb(positions, q, k)
+        trace_tensor_graph(f"{self.layer_name}.q_after_rope", q)
+        trace_tensor_graph(f"{self.layer_name}.k_after_rope", k)
+        with timed_region("minimax.attn.kv_attention"):
+            attn_output = self.attn(q, k, v)
+        trace_tensor_graph(f"{self.layer_name}.attn_output", attn_output)
+        with timed_region("minimax.attn.o_proj"):
+            output, _ = self.o_proj(attn_output)
+        trace_tensor_graph(f"{self.layer_name}.o_proj_output", output)
         return output
 
 
@@ -292,32 +733,70 @@ class MiniMaxM2DecoderLayer(nn.Module):
             quant_config=quant_config,
             prefix=f"{prefix}.mlp",
         )
+        self.tp_rank = get_tensor_model_parallel_rank()
+        self.tp_size = get_tensor_model_parallel_world_size()
         self.input_layernorm = RMSNorm(config.hidden_size, eps=config.rms_norm_eps)
         self.post_attention_layernorm = RMSNorm(
             config.hidden_size, eps=config.rms_norm_eps
         )
 
+    def _allreduce_with_rank0_residual(
+        self, hidden_states: torch.Tensor, residual: torch.Tensor
+    ) -> torch.Tensor:
+        if _MINIMAX_M2_DIST_RESIDUAL_ALLREDUCE:
+            hidden_states = hidden_states + residual * (1.0 / self.tp_size)
+            return tensor_model_parallel_all_reduce(hidden_states)
+        if self.tp_rank == 0:
+            hidden_states = hidden_states + residual
+        return tensor_model_parallel_all_reduce(hidden_states)
+
     def forward(
         self,
         positions: torch.Tensor,
         hidden_states: torch.Tensor,
         residual: torch.Tensor | None,
     ) -> torch.Tensor:
+        trace_tensor_graph(f"minimax.layer{self.layer_idx}.input", hidden_states)
         # Self Attention
         if residual is None:
             residual = hidden_states
             hidden_states = self.input_layernorm(hidden_states)
+        elif _MINIMAX_M2_MOE_DELAY_ALLREDUCE and self.tp_size > 1:
+            with timed_region("minimax.moe.delayed_residual_allreduce"):
+                hidden_states = self._allreduce_with_rank0_residual(
+                    hidden_states, residual
+                )
+            residual = hidden_states
+            hidden_states = self.input_layernorm(hidden_states)
         else:
             hidden_states, residual = self.input_layernorm(hidden_states, residual)
+        trace_tensor_graph(
+            f"minimax.layer{self.layer_idx}.after_input_norm", hidden_states
+        )
         hidden_states = self.self_attn(
             positions=positions,
             hidden_states=hidden_states,
         )
+        trace_tensor_graph(f"minimax.layer{self.layer_idx}.after_attn", hidden_states)
 
         # Fully Connected
-        hidden_states, residual = self.post_attention_layernorm(hidden_states, residual)
+        if _MINIMAX_M2_ATTN_DELAY_ALLREDUCE and self.tp_size > 1:
+            with timed_region("minimax.attn.delayed_residual_allreduce"):
+                hidden_states = self._allreduce_with_rank0_residual(
+                    hidden_states, residual
+                )
+            residual = hidden_states
+            hidden_states = self.post_attention_layernorm(hidden_states)
+        else:
+            hidden_states, residual = self.post_attention_layernorm(
+                hidden_states, residual
+            )
+        trace_tensor_graph(
+            f"minimax.layer{self.layer_idx}.after_post_attn_norm", hidden_states
+        )
 
         hidden_states = self.block_sparse_moe(hidden_states)
+        trace_tensor_graph(f"minimax.layer{self.layer_idx}.after_moe", hidden_states)
 
         return hidden_states, residual
 
@@ -334,6 +813,8 @@ class MiniMaxM2Model(nn.Module, EagleModelMixin):
         cache_config = vllm_config.cache_config
         quant_config = vllm_config.quant_config
         self.config = config
+        self.tp_rank = get_tensor_model_parallel_rank()
+        self.tp_size = get_tensor_model_parallel_world_size()
 
         self.vocab_size = config.vocab_size
 
@@ -401,7 +882,23 @@ class MiniMaxM2Model(nn.Module, EagleModelMixin):
             return IntermediateTensors(
                 {"hidden_states": hidden_states, "residual": residual}
             )
-        hidden_states, _ = self.norm(hidden_states, residual)
+        if (
+            _MINIMAX_M2_MOE_DELAY_ALLREDUCE
+            and self.tp_size > 1
+            and residual is not None
+        ):
+            if self.tp_rank == 0:
+                hidden_states = hidden_states + residual
+            hidden_states = tensor_model_parallel_all_reduce(hidden_states)
+            hidden_states = self.norm(hidden_states)
+        else:
+            hidden_states, _ = self.norm(hidden_states, residual)
+        trace_tensor_graph("minimax.model.final_hidden", hidden_states)
+        if (
+            os.environ.get("VLLM_MINIMAX_M2_CLONE_FINAL_HIDDEN", "0") == "1"
+            and hidden_states.device.type == "xpu"
+        ):
+            hidden_states = hidden_states.contiguous().clone()
 
         if len(aux_hidden_states) > 0:
             return hidden_states, aux_hidden_states
@@ -417,6 +914,22 @@ class MiniMaxM2Model(nn.Module, EagleModelMixin):
             num_experts=self.config.num_local_experts,
         )
 
+    def _init_fp16_router_buffers(self) -> None:
+        for layer in self.layers:
+            if not isinstance(layer, MiniMaxM2DecoderLayer):
+                continue
+            moe = layer.block_sparse_moe
+            gate_weight = getattr(moe.gate, "weight", None)
+            if gate_weight is None:
+                continue
+            gate_weight_fp16 = gate_weight.detach().to(torch.float16).contiguous()
+            if "_gate_weight_fp16" in moe._buffers:
+                moe._buffers["_gate_weight_fp16"] = gate_weight_fp16
+            else:
+                moe.register_buffer(
+                    "_gate_weight_fp16", gate_weight_fp16, persistent=False
+                )
+
     def load_weights(self, weights: Iterable[tuple[str, torch.Tensor]]) -> set[str]:
         stacked_params_mapping = [
             # (param_name, shard_name, shard_id)
@@ -513,6 +1026,12 @@ class MiniMaxM2Model(nn.Module, EagleModelMixin):
                     )
                     weight_loader(param, loaded_weight)
             loaded_params.add(name)
+        if (
+            _MINIMAX_M2_FP16_ROUTER
+            or _MINIMAX_M2_FP16_ROUTER_AUDIT
+            or _MINIMAX_M2_CANDIDATE_ROUTER_TOPM > 0
+        ):
+            self._init_fp16_router_buffers()
         return loaded_params
 
 
@@ -573,6 +1092,9 @@ class MiniMaxM2ForCausalLM(nn.Module, SupportsLoRA, SupportsPP, SupportsEagle3):
         logits = self.logits_processor(self.lm_head, hidden_states)
         return logits
 
+    def get_top_tokens(self, hidden_states: torch.Tensor) -> torch.Tensor:
+        return self.logits_processor.get_top_tokens(self.lm_head, hidden_states)
+
     def load_weights(self, weights: Iterable[tuple[str, torch.Tensor]]) -> set[str]:
         loader = AutoWeightsLoader(self)
         return loader.load_weights(weights)
diff --git a/vllm/v1/worker/gpu_model_runner.py b/vllm/v1/worker/gpu_model_runner.py
index bcab2ca2d..26da64e54 100644
--- a/vllm/v1/worker/gpu_model_runner.py
+++ b/vllm/v1/worker/gpu_model_runner.py
@@ -4,6 +4,7 @@
 import functools
 import gc
 import itertools
+import os
 import threading
 import time
 from collections import defaultdict
@@ -115,6 +116,7 @@ from vllm.utils.torch_utils import (
     is_quantized_kv_cache,
     kv_cache_dtype_str_to_dtype,
 )
+from vllm.utils.xpu_finite_trace import trace_tensor
 from vllm.v1.attention.backend import (
     AttentionBackend,
     AttentionCGSupport,
@@ -218,12 +220,39 @@ if TYPE_CHECKING:
 
 logger = init_logger(__name__)
 
+
+def _select_sample_hidden_states(
+    hidden_states: torch.Tensor, logits_indices: torch.Tensor
+) -> torch.Tensor:
+    if (
+        os.environ.get("VLLM_XPU_SAFE_SAMPLE_HIDDEN_SELECT", "0") == "1"
+        and hidden_states.device.type == "xpu"
+    ):
+        if hidden_states.shape[0] == logits_indices.numel():
+            return hidden_states
+        return torch.index_select(hidden_states, 0, logits_indices)
+    return hidden_states[logits_indices]
+
+
 AttnMetadataDict: TypeAlias = dict[str, AttentionMetadata]
 # list when ubatching is enabled
 PerLayerAttnMetadata: TypeAlias = list[AttnMetadataDict] | AttnMetadataDict
 
 
 # Wrapper for ModelRunnerOutput to support overlapped execution.
+class _CompletedCopyEvent:
+    def synchronize(self) -> None:
+        return None
+
+
+class _DeferredXpuOutputCopyEvent:
+    def __init__(self, output: "AsyncGPUModelRunnerOutput") -> None:
+        self.output = output
+
+    def synchronize(self) -> None:
+        self.output.finish_deferred_xpu_output_copy()
+
+
 class AsyncGPUModelRunnerOutput(AsyncModelRunnerOutput):
     def __init__(
         self,
@@ -245,6 +274,40 @@ class AsyncGPUModelRunnerOutput(AsyncModelRunnerOutput):
         self._sampled_token_ids = sampled_token_ids
         self.vocab_size = vocab_size
         self._logprobs_tensors = logprobs_tensors
+        self._deferred_xpu_output_copy = False
+
+        if (
+            os.environ.get("VLLM_XPU_DEFER_ASYNC_OUTPUT_COPY", "0") == "1"
+            and self._sampled_token_ids.device.type == "xpu"
+            and self._logprobs_tensors is None
+        ):
+            self.sampled_token_ids_cpu = torch.empty(
+                self._sampled_token_ids.shape,
+                dtype=self._sampled_token_ids.dtype,
+                device="cpu",
+            )
+            self._logprobs_tensors_cpu = None
+            self._deferred_xpu_output_copy = True
+            self.async_copy_ready_event = _DeferredXpuOutputCopyEvent(self)
+            return
+
+        if (
+            os.environ.get("VLLM_XPU_SYNC_ASYNC_OUTPUT_COPY", "0") == "1"
+            and self._sampled_token_ids.device.type == "xpu"
+        ):
+            self.sampled_token_ids_cpu = self._sampled_token_ids.to("cpu")
+            self._logprobs_tensors_cpu = (
+                LogprobsTensors(
+                    self._logprobs_tensors.logprob_token_ids.to("cpu"),
+                    self._logprobs_tensors.logprobs.to("cpu"),
+                    self._logprobs_tensors.selected_token_ranks.to("cpu"),
+                    self._logprobs_tensors.cu_num_generated_tokens,
+                )
+                if self._logprobs_tensors
+                else None
+            )
+            self.async_copy_ready_event = _CompletedCopyEvent()
+            return
 
         # Initiate the copy on a separate stream, but do not synchronize it.
         default_stream = torch.cuda.current_stream()
@@ -260,6 +323,15 @@ class AsyncGPUModelRunnerOutput(AsyncModelRunnerOutput):
             )
             self.async_copy_ready_event.record()
 
+    def finish_deferred_xpu_output_copy(self) -> None:
+        if not self._deferred_xpu_output_copy:
+            return
+        with torch.inference_mode():
+            if os.environ.get("VLLM_XPU_DEFERRED_COPY_SYNC_DEVICE", "1") != "0":
+                torch.xpu.synchronize()
+            self.sampled_token_ids_cpu.copy_(self._sampled_token_ids)
+        self._deferred_xpu_output_copy = False
+
     def get_output(self) -> ModelRunnerOutput:
         """Copy the device tensors to the host and return a ModelRunnerOutput.
 
@@ -1123,7 +1195,10 @@ class GPUModelRunner(
             self.speculative_config is not None
             and self.speculative_config.use_ngram_gpu()
         )
-        if is_ngram_gpu:
+        has_ngram_gpu_buffers = is_ngram_gpu and hasattr(
+            self, "token_ids_gpu_tensor"
+        )
+        if has_ngram_gpu_buffers:
             ngram_gpu_new_reqs: list[CachedRequestState] = []
 
         reqs_to_add: list[CachedRequestState] = []
@@ -1193,7 +1268,7 @@ class GPUModelRunner(
 
             reqs_to_add.append(req_state)
             # Track new requests for ngram_gpu full tensor copy
-            if is_ngram_gpu:
+            if has_ngram_gpu_buffers:
                 ngram_gpu_new_reqs.append(req_state)
 
         # Update the states of the running/resumed requests.
@@ -1208,6 +1283,9 @@ class GPUModelRunner(
             self.speculative_config is not None
             and self.speculative_config.use_ngram_gpu()
         ):
+            trace_ngram_pp = os.environ.get("VLLM_XPU_TRACE_NGRAM_PP")
+            original_total = scheduler_output.total_num_scheduled_tokens
+            original_num_scheduled = scheduler_output.num_scheduled_tokens.copy()
             for req_id, toks in scheduled_spec_tokens.items():
                 original_num_spec_per_req[req_id] = len(toks)
             update_scheduler_for_invalid_drafts(
@@ -1216,6 +1294,34 @@ class GPUModelRunner(
                 scheduler_output,
                 self.input_batch.req_id_to_index,
             )
+            if trace_ngram_pp:
+                valid_counts: dict[str, int] = {}
+                if self._num_valid_draft_tokens_cpu is not None:
+                    for req_id in req_data.req_ids:
+                        req_index = self.input_batch.req_id_to_index.get(req_id)
+                        if req_index is not None:
+                            valid_counts[req_id] = int(
+                                self._num_valid_draft_tokens_cpu[req_index].item()
+                            )
+                logger.warning(
+                    "XPU ngram trim pp_rank=%s first=%s last=%s "
+                    "total=%s->%s scheduled=%s->%s spec_lens=%s->%s "
+                    "valid_counts=%s cached_req_ids=%s",
+                    get_pp_group().rank_in_group,
+                    get_pp_group().is_first_rank,
+                    get_pp_group().is_last_rank,
+                    original_total,
+                    scheduler_output.total_num_scheduled_tokens,
+                    original_num_scheduled,
+                    scheduler_output.num_scheduled_tokens,
+                    original_num_spec_per_req,
+                    {
+                        req_id: len(toks)
+                        for req_id, toks in scheduled_spec_tokens.items()
+                    },
+                    valid_counts,
+                    req_data.req_ids,
+                )
         if self.use_async_spec_decode:
             self.prev_num_draft_tokens.np.fill(0)
 
@@ -1264,7 +1370,7 @@ class GPUModelRunner(
                             optimistic_num_accepted
                         )
 
-                    if is_ngram_gpu and optimistic_num_accepted > 0:
+                    if has_ngram_gpu_buffers and optimistic_num_accepted > 0:
                         self.input_batch.num_tokens_no_spec[req_index] += (
                             optimistic_num_accepted
                         )
@@ -1286,13 +1392,32 @@ class GPUModelRunner(
                     num_new_tokens = (
                         num_computed_tokens + len(new_token_ids) - req_state.num_tokens
                     )
-                    if num_new_tokens == 1:
-                        # Avoid slicing list in most common case.
-                        req_state.output_token_ids.append(new_token_ids[-1])
-                    elif num_new_tokens > 0:
-                        req_state.output_token_ids.extend(
-                            new_token_ids[-num_new_tokens:]
-                        )
+                    if num_new_tokens > 0:
+                        if os.environ.get("VLLM_XPU_TRACE_NGRAM_PP") and (
+                            num_new_tokens > len(new_token_ids)
+                        ):
+                            logger.warning(
+                                "XPU PP state update has fewer token ids than "
+                                "computed delta pp_rank=%s req_id=%s "
+                                "num_new_tokens=%s new_token_ids=%s "
+                                "num_computed_tokens=%s req_state_num_tokens=%s "
+                                "scheduled=%s spec_lens=%s",
+                                get_pp_group().rank_in_group,
+                                req_id,
+                                num_new_tokens,
+                                new_token_ids,
+                                num_computed_tokens,
+                                req_state.num_tokens,
+                                scheduler_output.num_scheduled_tokens,
+                                {
+                                    spec_req_id: len(toks)
+                                    for spec_req_id, toks in scheduled_spec_tokens.items()
+                                },
+                            )
+                        if new_token_ids:
+                            req_state.output_token_ids.extend(
+                                new_token_ids[-num_new_tokens:]
+                            )
             elif num_output_tokens < len(req_state.output_token_ids):
                 # Some output tokens were discarded due to a sync-KV-load
                 # failure, or output_token_ids was inflated by the optimistic
@@ -1331,7 +1456,7 @@ class GPUModelRunner(
 
                 reqs_to_add.append(req_state)
                 # Track resumed requests for ngram_gpu full tensor copy
-                if is_ngram_gpu:
+                if has_ngram_gpu_buffers:
                     ngram_gpu_new_reqs.append(req_state)
                 continue
 
@@ -1373,7 +1498,7 @@ class GPUModelRunner(
         self.input_batch.refresh_metadata()
 
         # Incrementally update ngram_gpu tensors after batch is stable
-        if is_ngram_gpu:
+        if has_ngram_gpu_buffers:
             update_ngram_gpu_tensors_incremental(
                 self.input_batch,
                 self.token_ids_gpu_tensor,
@@ -1410,7 +1535,7 @@ class GPUModelRunner(
                     self.input_batch.num_computed_tokens_cpu[cur_req_index] -= (
                         correction
                     )
-                    if is_ngram_gpu and correction > 0:
+                    if has_ngram_gpu_buffers and correction > 0:
                         self.input_batch.num_tokens_no_spec[cur_req_index] -= correction
                         self.num_tokens_no_spec_gpu[cur_req_index] -= correction
 
@@ -2310,8 +2435,9 @@ class GPUModelRunner(
                 cm.slot_mapping = slot_mappings[kv_cache_gid]
 
             if self.speculative_config and spec_decode_common_attn_metadata is None:
-                if isinstance(self.drafter, (EagleProposer, DFlashProposer)):
-                    if self.drafter.kv_cache_gid == kv_cache_gid:
+                drafter = getattr(self, "drafter", None)
+                if isinstance(drafter, (EagleProposer, DFlashProposer)):
+                    if drafter.kv_cache_gid == kv_cache_gid:
                         spec_decode_common_attn_metadata = cm
                 else:
                     spec_decode_common_attn_metadata = cm
@@ -3364,11 +3490,35 @@ class GPUModelRunner(
             ec_connector_output,
         )
 
+    def _xpu_local_argmax_debug(self, message: str) -> None:
+        if os.environ.get("VLLM_XPU_LOCAL_ARGMAX_DEBUG", "0") != "1":
+            return
+        count = getattr(self, "_xpu_local_argmax_debug_count", 0)
+        limit = int(os.environ.get("VLLM_XPU_LOCAL_ARGMAX_DEBUG_LIMIT", "24"))
+        if count >= limit:
+            return
+        self._xpu_local_argmax_debug_count = count + 1
+        rank = -1
+        if torch.distributed.is_available() and torch.distributed.is_initialized():
+            rank = torch.distributed.get_rank()
+        logger.info("XPU local argmax rank=%s: %s", rank, message)
+
     def _sample(
         self,
         logits: torch.Tensor | None,
         spec_decode_metadata: SpecDecodeMetadata | None,
     ) -> SamplerOutput:
+        precomputed_sampled_token_ids = getattr(
+            self, "_xpu_local_argmax_sampled_token_ids", None
+        )
+        if logits is None and precomputed_sampled_token_ids is not None:
+            self._xpu_local_argmax_sampled_token_ids = None
+            self._xpu_local_argmax_debug("using precomputed sampled_token_ids")
+            return SamplerOutput(
+                sampled_token_ids=precomputed_sampled_token_ids,
+                logprobs_tensors=None,
+            )
+
         # Sample the next token and get logprobs if needed.
         sampling_metadata = self.input_batch.sampling_metadata
         # Update output token ids with tokens sampled in last step
@@ -3394,6 +3544,60 @@ class GPUModelRunner(
         )
         return sampler_output
 
+    def _can_use_xpu_local_argmax(
+        self,
+        spec_decode_metadata: SpecDecodeMetadata | None,
+    ) -> bool:
+        def disabled(reason: str) -> bool:
+            self._xpu_local_argmax_debug(f"disabled: {reason}")
+            return False
+
+        if os.environ.get("VLLM_XPU_LOCAL_ARGMAX_DECODE", "0") != "1":
+            return disabled("env flag is not enabled")
+        if spec_decode_metadata is not None:
+            return disabled("spec decode metadata is present")
+        sampling_metadata = self.input_batch.sampling_metadata
+        if not sampling_metadata.all_greedy:
+            return disabled("not all requests are greedy")
+        if sampling_metadata.max_num_logprobs is not None:
+            return disabled("logprobs requested")
+        if sampling_metadata.logprob_token_ids:
+            return disabled("specific logprob token IDs requested")
+        if not sampling_metadata.no_penalties:
+            return disabled("sampling penalties are active")
+        if sampling_metadata.allowed_token_ids_mask is not None:
+            return disabled("allowed-token mask is active")
+        if sampling_metadata.bad_words_token_ids:
+            return disabled("bad-word filtering is active")
+        active_processors: list[str] = []
+        for processor in sampling_metadata.logitsprocs.non_argmax_invariant:
+            name = processor.__class__.__name__
+            if name == "LogitBiasLogitsProcessor":
+                if getattr(processor, "biases", None):
+                    active_processors.append(name)
+            elif name == "MinTokensLogitsProcessor":
+                if getattr(processor, "min_toks", None):
+                    active_processors.append(name)
+            elif name == "ThinkingTokenBudgetLogitsProcessor":
+                if getattr(processor, "is_enabled", False) and getattr(
+                    processor, "_state", None
+                ):
+                    active_processors.append(name)
+            else:
+                active_processors.append(name)
+        if active_processors:
+            return disabled(
+                "active non-argmax logits processors: "
+                + ",".join(active_processors)
+            )
+        holder = sampling_metadata.thinking_budget_state_holder
+        if holder is not None and holder.has_tracked_requests():
+            return disabled("thinking-budget tracking is active")
+        if not hasattr(self.model, "get_top_tokens"):
+            return disabled("model has no get_top_tokens method")
+        self._xpu_local_argmax_debug("enabled")
+        return True
+
     def _bookkeeping_sync(
         self,
         scheduler_output: "SchedulerOutput",
@@ -3869,6 +4073,23 @@ class GPUModelRunner(
         ):
             # Update persistent batch states.
             deferred_state_corrections_fn = self._update_states(scheduler_output)
+            if os.environ.get("VLLM_XPU_TRACE_NGRAM_PP"):
+                logger.warning(
+                    "XPU ngram post-update pp_rank=%s first=%s last=%s "
+                    "pre_update_total=%s post_update_total=%s scheduled=%s "
+                    "spec_lens=%s req_ids=%s",
+                    get_pp_group().rank_in_group,
+                    get_pp_group().is_first_rank,
+                    get_pp_group().is_last_rank,
+                    num_scheduled_tokens,
+                    scheduler_output.total_num_scheduled_tokens,
+                    scheduler_output.num_scheduled_tokens,
+                    {
+                        req_id: len(toks)
+                        for req_id, toks in scheduler_output.scheduled_spec_decode_tokens.items()
+                    },
+                    self.input_batch.req_ids,
+                )
 
             if has_ec_transfer() and not get_ec_transfer().is_consumer:
                 with self.maybe_get_ec_connector_output(
@@ -4062,6 +4283,10 @@ class GPUModelRunner(
         has_encoder_input = (
             self.model_config.is_encoder_decoder and num_encoder_reqs > 0
         )
+        skip_compiled = has_encoder_input or (
+            os.environ.get("VLLM_XPU_SKIP_COMPILED_PREFILL", "0") == "1"
+            and num_tokens_padded > 1
+        )
 
         # Run the model.
         # Use persistent buffers for CUDA graphs.
@@ -4078,7 +4303,7 @@ class GPUModelRunner(
                 batch_descriptor=batch_desc,
                 ubatch_slices=ubatch_slices_padded,
                 slot_mapping=slot_mappings,
-                skip_compiled=has_encoder_input,
+                skip_compiled=skip_compiled,
             ),
             record_function_or_nullcontext("gpu_model_runner: forward"),
             self.maybe_get_kv_connector_output(
@@ -4093,6 +4318,11 @@ class GPUModelRunner(
                 inputs_embeds=inputs_embeds,
                 **model_kwargs,
             )
+            if (
+                os.environ.get("VLLM_XPU_SYNC_AFTER_MODEL_FORWARD", "0") == "1"
+                and torch.xpu.is_available()
+            ):
+                torch.xpu.synchronize()
 
         with record_function_or_nullcontext("gpu_model_runner: postprocess"):
             if self.use_aux_hidden_state_outputs:
@@ -4121,13 +4351,43 @@ class GPUModelRunner(
                         kv_connector_output,
                     )
 
-                sample_hidden_states = hidden_states[logits_indices]
-                logits = self.model.compute_logits(sample_hidden_states)
+                trace_tensor(
+                    "gpu_model_runner.hidden_states_before_logits_index",
+                    hidden_states,
+                )
+                trace_tensor("gpu_model_runner.logits_indices", logits_indices)
+                sample_hidden_states = _select_sample_hidden_states(
+                    hidden_states, logits_indices
+                )
+                trace_tensor(
+                    "gpu_model_runner.sample_hidden_states", sample_hidden_states
+                )
+                if self._can_use_xpu_local_argmax(spec_decode_metadata):
+                    sampled_token_ids = self.model.get_top_tokens(sample_hidden_states)
+                    self._xpu_local_argmax_sampled_token_ids = sampled_token_ids.to(
+                        torch.int32
+                    ).unsqueeze(-1)
+                    trace_tensor(
+                        "gpu_model_runner.local_argmax_sampled_token_ids",
+                        self._xpu_local_argmax_sampled_token_ids,
+                    )
+                    logits = None
+                else:
+                    self._xpu_local_argmax_sampled_token_ids = None
+                    logits = self.model.compute_logits(sample_hidden_states)
+                    trace_tensor("gpu_model_runner.logits_after_compute", logits)
             else:
                 # Rare case.
                 assert not self.is_pooling_model
 
-                sample_hidden_states = hidden_states[logits_indices]
+                trace_tensor(
+                    "gpu_model_runner.hidden_states_before_logits_index",
+                    hidden_states,
+                )
+                trace_tensor("gpu_model_runner.logits_indices", logits_indices)
+                sample_hidden_states = _select_sample_hidden_states(
+                    hidden_states, logits_indices
+                )
                 if not get_pp_group().is_last_rank:
                     all_gather_tensors = {
                         "residual": not is_residual_scattered_for_sp(
@@ -4142,6 +4402,7 @@ class GPUModelRunner(
                     logits = None
                 else:
                     logits = self.model.compute_logits(sample_hidden_states)
+                    trace_tensor("gpu_model_runner.logits_after_compute", logits)
 
                 model_output_broadcast_data: dict[str, Any] = {}
                 if logits is not None:
@@ -4253,6 +4514,36 @@ class GPUModelRunner(
                     spec_decode_common_attn_metadata,
                     slot_mappings,
                 )
+                if os.environ.get("VLLM_XPU_TRACE_NGRAM_PP"):
+                    if torch.is_tensor(self._draft_token_ids):
+                        draft_summary = {
+                            "shape": tuple(self._draft_token_ids.shape),
+                            "dtype": str(self._draft_token_ids.dtype),
+                        }
+                    else:
+                        draft_summary = [
+                            len(toks) for toks in self._draft_token_ids[:4]
+                        ]
+                    if torch.is_tensor(sampled_token_ids):
+                        sampled_summary = {
+                            "shape": tuple(sampled_token_ids.shape),
+                            "dtype": str(sampled_token_ids.dtype),
+                        }
+                    else:
+                        sampled_summary = [
+                            len(toks) for toks in sampled_token_ids[:4]
+                        ]
+                    logger.warning(
+                        "XPU ngram proposed pp_rank=%s first=%s last=%s "
+                        "method=%s sampled=%s draft=%s req_ids=%s",
+                        get_pp_group().rank_in_group,
+                        get_pp_group().is_first_rank,
+                        get_pp_group().is_last_rank,
+                        spec_config.method,
+                        sampled_summary,
+                        draft_summary,
+                        self.input_batch.req_ids,
+                    )
                 self._copy_draft_token_ids_to_cpu(scheduler_output)
 
         spec_config = self.speculative_config
@@ -4459,8 +4750,28 @@ class GPUModelRunner(
 
     def take_draft_token_ids(self) -> DraftTokenIds | None:
         if not self.num_spec_tokens or not self._draft_token_req_ids:
+            if os.environ.get("VLLM_XPU_TRACE_NGRAM_PP"):
+                logger.warning(
+                    "XPU ngram take_draft none pp_rank=%s first=%s last=%s "
+                    "num_spec_tokens=%s has_req_ids=%s",
+                    get_pp_group().rank_in_group,
+                    get_pp_group().is_first_rank,
+                    get_pp_group().is_last_rank,
+                    self.num_spec_tokens,
+                    bool(self._draft_token_req_ids),
+                )
             return None
         draft_token_ids, req_ids = self._get_draft_token_ids_cpu()
+        if os.environ.get("VLLM_XPU_TRACE_NGRAM_PP"):
+            logger.warning(
+                "XPU ngram take_draft pp_rank=%s first=%s last=%s "
+                "req_ids=%s draft_lens=%s",
+                get_pp_group().rank_in_group,
+                get_pp_group().is_first_rank,
+                get_pp_group().is_last_rank,
+                req_ids,
+                [len(toks) for toks in draft_token_ids[:4]],
+            )
         return DraftTokenIds(req_ids, draft_token_ids)
 
     def _copy_draft_token_ids_to_cpu(
@@ -5570,6 +5881,11 @@ class GPUModelRunner(
                     batch_descriptor=batch_desc,
                     ubatch_slices=ubatch_slices_padded,
                     slot_mapping=slot_mappings,
+                    skip_compiled=(
+                        os.environ.get("VLLM_XPU_SKIP_COMPILED_PREFILL", "0")
+                        == "1"
+                        and num_tokens_padded > 1
+                    ),
                 ),
             ):
                 outputs = self.model(
@@ -7041,6 +7357,13 @@ class GPUModelRunner(
         # this is in the critical path of every single model
         # forward loop, this has caused perf issue for a disagg
         # setup.
+        if (
+            sampled_token_ids.device.type == "xpu"
+            and os.environ.get("VLLM_XPU_SYNC_TO_LIST", "0") == "1"
+        ):
+            if os.environ.get("VLLM_XPU_SYNC_TO_LIST_BEFORE_COPY", "1") != "0":
+                torch.xpu.synchronize()
+            return sampled_token_ids.to("cpu").tolist()
         pinned = self.sampled_token_ids_pinned_cpu[: sampled_token_ids.shape[0]]
         pinned.copy_(sampled_token_ids, non_blocking=True)
         self.transfer_event.record()
