diff --git a/vllm/v1/worker/xpu_worker.py b/vllm/v1/worker/xpu_worker.py
--- a/vllm/v1/worker/xpu_worker.py
+++ b/vllm/v1/worker/xpu_worker.py
@@ -73,9 +73,21 @@ class XPUWorker(Worker):
         ENV_LOCAL_WORLD_SIZE = os.getenv(
             "LOCAL_WORLD_SIZE", str(self.parallel_config.world_size)
         )
+        tp_pp_world_size = (
+            self.parallel_config.pipeline_parallel_size
+            * self.parallel_config.tensor_parallel_size
+        )
+        dp_local_rank = self.parallel_config.data_parallel_rank_local
+        if dp_local_rank is None:
+            dp_local_rank = self.parallel_config.data_parallel_index
+        ccl_local_rank = dp_local_rank * tp_pp_world_size + self.local_rank
+        ccl_local_size = self.parallel_config.data_parallel_size_local * tp_pp_world_size
         os.environ["CCL_ATL_TRANSPORT"] = ENV_CCL_ATL_TRANSPORT
         os.environ["LOCAL_WORLD_SIZE"] = ENV_LOCAL_WORLD_SIZE
         os.environ["LOCAL_RANK"] = str(self.local_rank)
+        os.environ.setdefault("CCL_PROCESS_LAUNCHER", "none")
+        os.environ["CCL_LOCAL_RANK"] = str(ccl_local_rank)
+        os.environ["CCL_LOCAL_SIZE"] = str(ccl_local_size)
 
         init_worker_distributed_environment(
             self.vllm_config,
diff --git a/vllm/model_executor/layers/quantization/moe_wna16.py b/vllm/model_executor/layers/quantization/moe_wna16.py
--- a/vllm/model_executor/layers/quantization/moe_wna16.py
+++ b/vllm/model_executor/layers/quantization/moe_wna16.py
@@ -476,6 +476,9 @@ class MoeWNA16Method(FusedMoEMethodBase):
                     local_mask = mapped_topk_ids >= 0
                     mapped_topk_weights = topk_weights * local_mask.to(
                         topk_weights.dtype
                     )
+                    mapped_topk_ids = torch.where(
+                        local_mask, mapped_topk_ids, torch.zeros_like(mapped_topk_ids)
+                    )
                 else:
                     mapped_topk_ids = topk_ids
                     mapped_topk_weights = topk_weights
