diff --git a/vllm/envs.py b/vllm/envs.py
index b2db5a811..9fcfe3c34 100755
--- a/vllm/envs.py
+++ b/vllm/envs.py
@@ -265,6 +265,8 @@ if TYPE_CHECKING:
     VLLM_MEMORY_PROFILER_ESTIMATE_CUDAGRAPHS: bool = True
     VLLM_NIXL_EP_MAX_NUM_RANKS: int = 32
     VLLM_XPU_ENABLE_XPU_GRAPH: bool = False
+    VLLM_XPU_BLOCK_FP8_REQUANT: bool = False
+    VLLM_QWEN35_MTP_FORCE_FP8_BLOCK: bool = False
     VLLM_LORA_ENABLE_DUAL_STREAM: bool = False
 
 
@@ -1769,6 +1771,16 @@ environment_variables: dict[str, Callable[[], Any]] = {
     "VLLM_XPU_ENABLE_XPU_GRAPH": lambda: bool(
         int(os.getenv("VLLM_XPU_ENABLE_XPU_GRAPH", "0"))
     ),
+    # Experimental Intel XPU path that requantizes block-FP8 checkpoints to
+    # XPU W8A16 FP8 GEMM format.
+    "VLLM_XPU_BLOCK_FP8_REQUANT": lambda: bool(
+        int(os.getenv("VLLM_XPU_BLOCK_FP8_REQUANT", "0"))
+    ),
+    # Local experimental override for hybrid Qwen3.5 MTP checkpoints where the
+    # target model uses compressed-tensors FP8 but mtp.safetensors is block-FP8.
+    "VLLM_QWEN35_MTP_FORCE_FP8_BLOCK": lambda: bool(
+        int(os.getenv("VLLM_QWEN35_MTP_FORCE_FP8_BLOCK", "0"))
+    ),
     # Enable simple KV offload.
     "VLLM_USE_SIMPLE_KV_OFFLOAD": lambda: bool(
         int(os.getenv("VLLM_USE_SIMPLE_KV_OFFLOAD", "0"))
diff --git a/vllm/model_executor/models/qwen3_5_mtp.py b/vllm/model_executor/models/qwen3_5_mtp.py
index e86b205b9..9cac4d954 100644
--- a/vllm/model_executor/models/qwen3_5_mtp.py
+++ b/vllm/model_executor/models/qwen3_5_mtp.py
@@ -2,8 +2,10 @@
 # SPDX-FileCopyrightText: Copyright contributors to the vLLM project
 """Inference-only Qwen3_5 MTP model."""
 
+import os
 import typing
 from collections.abc import Callable, Iterable
+from copy import copy
 
 import torch
 from torch import nn
@@ -16,6 +18,7 @@ from vllm.model_executor.layers.fused_moe import (
     fused_moe_make_expert_params_mapping,
 )
 from vllm.model_executor.layers.linear import ColumnParallelLinear
+from vllm.model_executor.layers.quantization.fp8 import Fp8Config
 from vllm.model_executor.layers.logits_processor import LogitsProcessor
 from vllm.model_executor.layers.vocab_parallel_embedding import (
     ParallelLMHead,
@@ -44,6 +47,32 @@ from .utils import (
 
 logger = init_logger(__name__)
 
+_QWEN3_5_MTP_PACKED_MODULES_MAPPING = {
+    "qkv_proj": [
+        "q_proj",
+        "k_proj",
+        "v_proj",
+    ],
+    "gate_up_proj": ["gate_proj", "up_proj"],
+}
+
+
+def _mtp_prefix(prefix: str, suffix: str) -> str:
+    return f"{prefix}.{suffix}" if prefix else suffix
+
+
+def _make_block_fp8_mtp_config(prefix: str) -> Fp8Config:
+    quant_config = Fp8Config(
+        is_checkpoint_fp8_serialized=True,
+        activation_scheme="dynamic",
+        ignored_layers=[
+            _mtp_prefix(prefix, "fc"),
+        ],
+        weight_block_size=[128, 128],
+    )
+    quant_config.packed_modules_mapping = dict(_QWEN3_5_MTP_PACKED_MODULES_MAPPING)
+    return quant_config
+
 
 @support_torch_compile(
     dynamic_arg_dims={
@@ -62,6 +91,7 @@ class Qwen3_5MultiTokenPredictor(nn.Module):
 
         model_config = vllm_config.model_config
         quant_config = vllm_config.quant_config
+        mtp_vllm_config = vllm_config
 
         config: Qwen3_5TextConfig | Qwen3_5MoeTextConfig = model_config.hf_text_config
 
@@ -77,6 +107,15 @@ class Qwen3_5MultiTokenPredictor(nn.Module):
             config.hidden_size,
         )
 
+        if os.getenv("VLLM_QWEN35_MTP_FORCE_FP8_BLOCK", "0") == "1":
+            quant_config = _make_block_fp8_mtp_config(prefix)
+            mtp_vllm_config = copy(vllm_config)
+            mtp_vllm_config.quant_config = quant_config
+            logger.warning_once(
+                "Forcing Qwen3.5 MTP drafter linear layers to block-FP8 "
+                "quantization via VLLM_QWEN35_MTP_FORCE_FP8_BLOCK=1."
+            )
+
         # Workaround: mtp.fc is stored as BF16 in NVFP4 checkpoints but is
         # missing from hf_quant_config.json exclude_modules. Force unquantized.
         # Ref: https://github.com/vllm-project/vllm/pull/38650
@@ -98,7 +137,7 @@ class Qwen3_5MultiTokenPredictor(nn.Module):
 
         self.layers = torch.nn.ModuleList(
             Qwen3_5DecoderLayer(
-                vllm_config,
+                mtp_vllm_config,
                 layer_type="full_attention",
                 prefix=f"{prefix}.layers.{idx}",
             )
@@ -223,18 +262,22 @@ class Qwen3_5MultiTokenPredictor(nn.Module):
             if "rotary_emb.inv_freq" in name:
                 continue
 
+            loaded_name = name
             for param_name, weight_name, shard_id in stacked_params_mapping:
-                if "experts.gate_up_proj" in name or "experts.down_proj" in name:
+                if (
+                    "experts.gate_up_proj" in loaded_name
+                    or "experts.down_proj" in loaded_name
+                ):
                     is_fused_expert = True
                     expert_params_mapping = fused_expert_params_mapping
 
-                if weight_name not in name:
+                if weight_name not in loaded_name:
                     continue
 
-                if "mlp.experts" in name:
+                if "mlp.experts" in loaded_name:
                     continue
 
-                name = name.replace(weight_name, param_name)
+                name = loaded_name.replace(weight_name, param_name)
                 # Skip loading extra bias for GPTQ models.
                 if name.endswith(".bias") and name not in params_dict:
                     continue
@@ -347,14 +390,7 @@ class Qwen3_5MultiTokenPredictor(nn.Module):
     }
 )
 class Qwen3_5MTP(nn.Module, SupportsMultiModal):
-    packed_modules_mapping = {
-        "qkv_proj": [
-            "q_proj",
-            "k_proj",
-            "v_proj",
-        ],
-        "gate_up_proj": ["gate_proj", "up_proj"],
-    }
+    packed_modules_mapping = _QWEN3_5_MTP_PACKED_MODULES_MAPPING
 
     def __init__(self, *, vllm_config: VllmConfig, prefix: str = ""):
         config = vllm_config.model_config.hf_text_config
