diff --git a/ggml/src/ggml-sycl/ggml-sycl.cpp b/ggml/src/ggml-sycl/ggml-sycl.cpp
--- a/ggml/src/ggml-sycl/ggml-sycl.cpp
+++ b/ggml/src/ggml-sycl/ggml-sycl.cpp
@@
 int g_ggml_sycl_prioritize_dmmv = 0;
 int g_ggml_sycl_dmmv_output = 0;
+int g_ggml_sycl_force_mmvq_q4_1 = 0;
 int g_ggml_sycl_use_async_mem_op = 0;
@@
         g_ggml_sycl_prioritize_dmmv = get_sycl_env("GGML_SYCL_PRIORITIZE_DMMV", 0);
         g_ggml_sycl_dmmv_output = get_sycl_env("GGML_SYCL_DMMV_OUTPUT", 0);
+        g_ggml_sycl_force_mmvq_q4_1 = get_sycl_env("GGML_SYCL_FORCE_MMVQ_Q4_1", 0);
         g_ggml_sycl_async_peer_copy = get_sycl_env("GGML_SYCL_ASYNC_PEER_COPY", 0);
@@
         GGML_LOG_INFO("  GGML_SYCL_PRIORITIZE_DMMV: %d\n", g_ggml_sycl_prioritize_dmmv);
         GGML_LOG_INFO("  GGML_SYCL_DMMV_OUTPUT: %d\n", g_ggml_sycl_dmmv_output);
+        GGML_LOG_INFO("  GGML_SYCL_FORCE_MMVQ_Q4_1: %d\n", g_ggml_sycl_force_mmvq_q4_1);
         GGML_LOG_INFO("  GGML_SYCL_ASYNC_PEER_COPY: %d\n", g_ggml_sycl_async_peer_copy);
@@
     if (!prioritize_dmmv && ((should_reorder_tensor(ctx, dst) &&
                                           ggml_sycl_supports_reorder_mmvq(src0->type)))) {
       // Arc770 get benefit with Q4_0 by skipping it.
       if (!(ggml_sycl_info().devices[ctx.device].hw_info.arch ==
                 gpu_arch::intel_gpu_acm_g10 &&
             src0->type == GGML_TYPE_Q4_0)) {
         use_dequantize_mul_mat_vec =
             use_dequantize_mul_mat_vec && !use_mul_mat_vec_q;
       }
     }
+
+    if (g_ggml_sycl_force_mmvq_q4_1 && src0->type == GGML_TYPE_Q4_1 && use_mul_mat_vec_q) {
+        use_dequantize_mul_mat_vec = false;
+    }
 
     if (!split && src0->type == GGML_TYPE_F16 && ggml_is_permuted(src0) && ggml_is_permuted(src1) && src1->ne[1] == 1) {
diff --git a/ggml/src/ggml-sycl/mmvq.cpp b/ggml/src/ggml-sycl/mmvq.cpp
--- a/ggml/src/ggml-sycl/mmvq.cpp
+++ b/ggml/src/ggml-sycl/mmvq.cpp
@@
             case GGML_TYPE_Q4_1:
+                GGML_SYCL_DEBUG("Calling mul_mat_vec_q4_1_q8_1_sycl\n");
                 mul_mat_vec_q4_1_q8_1_sycl(src0_dd_i, src1_ddq_i_bs, dst_dd_i_bs, ne00, row_diff, stream);
                 break;
