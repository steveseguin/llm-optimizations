diff --git a/vllm/model_executor/layers/fused_moe/router/base_router.py b/vllm/model_executor/layers/fused_moe/router/base_router.py
index 0138eb59c..03e251eec 100644
--- a/vllm/model_executor/layers/fused_moe/router/base_router.py
+++ b/vllm/model_executor/layers/fused_moe/router/base_router.py
@@ -280,10 +280,16 @@ class BaseRouter(FusedMoERouter):
         # Step 2: Get indices type.
         indices_type = self._get_indices_type()
 
-        # Step 3: Compute routing (delegated to subclass)
-        topk_weights, topk_ids = self._compute_routing(
-            hidden_states, router_logits, indices_type, input_ids=input_ids
-        )
+        # Step 3: Compute routing (delegated to subclass), unless a
+        # model-specific path has supplied exact top-k routing for this call.
+        precomputed_topk = getattr(self, "_precomputed_topk", None)
+        if precomputed_topk is not None:
+            self._precomputed_topk = None
+            topk_weights, topk_ids = precomputed_topk
+        else:
+            topk_weights, topk_ids = self._compute_routing(
+                hidden_states, router_logits, indices_type, input_ids=input_ids
+            )
 
         # Capture logical ids before EPLB mapping.
         if self.capture_fn is not None:
diff --git a/vllm/model_executor/layers/fused_moe/runner/moe_runner.py b/vllm/model_executor/layers/fused_moe/runner/moe_runner.py
index 2eee8acf6..50ff92ad0 100644
--- a/vllm/model_executor/layers/fused_moe/runner/moe_runner.py
+++ b/vllm/model_executor/layers/fused_moe/runner/moe_runner.py
@@ -1,5 +1,6 @@
 # SPDX-License-Identifier: Apache-2.0
 # SPDX-FileCopyrightText: Copyright contributors to the vLLM project
+import os
 from collections.abc import Callable
 from contextlib import nullcontext
 from typing import TYPE_CHECKING
@@ -44,6 +45,10 @@ from vllm.utils.torch_utils import (
 )
 
 
+def _minimax_moe_delay_allreduce_enabled() -> bool:
+    return os.environ.get("VLLM_MINIMAX_MOE_DELAY_ALLREDUCE", "0") == "1"
+
+
 def get_layer_from_name(layer_name: str) -> torch.nn.Module:
     forward_context: ForwardContext = get_forward_context()
     if not _USE_LAYERNAME and layer_name == "from_forward_context":
@@ -373,6 +378,7 @@ class MoERunner(MoERunnerInterface):
             not self.moe_config.is_sequence_parallel
             and (self.moe_config.tp_size > 1 or self.moe_config.ep_size > 1)
             and not self._fused_output_is_reduced
+            and not _minimax_moe_delay_allreduce_enabled()
         ):
             states = tensor_model_parallel_all_reduce(states)
 
@@ -451,7 +457,20 @@ class MoERunner(MoERunnerInterface):
             shared_experts_input, SharedExpertsOrder.NO_OVERLAP
         )
 
-        if self._quant_method.is_monolithic:
+        should_apply_decode_monolithic = getattr(
+            self._quant_method, "should_apply_decode_monolithic", None
+        )
+        if (
+            should_apply_decode_monolithic is not None
+            and should_apply_decode_monolithic(layer, hidden_states)
+        ):
+            fused_out = self._quant_method.apply_monolithic(
+                layer=layer,
+                x=hidden_states,
+                router_logits=router_logits,
+                input_ids=input_ids,
+            )
+        elif self._quant_method.is_monolithic:
             fused_out = self._quant_method.apply_monolithic(
                 layer=layer,
                 x=hidden_states,
diff --git a/vllm/model_executor/layers/quantization/moe_wna16.py b/vllm/model_executor/layers/quantization/moe_wna16.py
index e5ef3f4c3..00e9bc812 100644
--- a/vllm/model_executor/layers/quantization/moe_wna16.py
+++ b/vllm/model_executor/layers/quantization/moe_wna16.py
@@ -1,11 +1,13 @@
 # SPDX-License-Identifier: Apache-2.0
 # SPDX-FileCopyrightText: Copyright contributors to the vLLM project
 
+import os
 from typing import Any
 
 import torch
 
 from vllm.distributed import get_tensor_model_parallel_rank, get_tp_group
+from vllm.logger import init_logger
 from vllm.model_executor.layers.fused_moe.activation import MoEActivation
 from vllm.model_executor.layers.fused_moe.config import (
     FusedMoEQuantConfig,
@@ -32,6 +34,7 @@ from vllm.model_executor.layers.quantization.utils.marlin_utils import (
 )
 from vllm.model_executor.utils import set_weight_attrs
 from vllm.platforms import current_platform
+logger = init_logger(__name__)
 
 
 class MoeWNA16Config(QuantizationConfig):
@@ -362,6 +365,104 @@ class MoeWNA16Method(FusedMoEMethodBase):
             block_shape=[0, layer.group_size],
         )
 
+    def _llm_scaler_moe_requested(self) -> bool:
+        return os.environ.get("VLLM_XPU_USE_LLM_SCALER_MOE", "0") == "1"
+
+    def _llm_scaler_moe_logits_requested(self) -> bool:
+        return os.environ.get("VLLM_XPU_USE_LLM_SCALER_MOE_LOGITS", "0") == "1"
+
+    def _llm_scaler_moe_minimax_logits_requested(self) -> bool:
+        return (
+            os.environ.get("VLLM_XPU_USE_LLM_SCALER_MOE_MINIMAX_LOGITS", "0")
+            == "1"
+        )
+
+    def _llm_scaler_moe_ws_requested(self) -> bool:
+        return os.environ.get("VLLM_XPU_USE_LLM_SCALER_MOE_WS", "0") == "1"
+
+    @property
+    def is_monolithic(self) -> bool:
+        return False
+
+    def should_apply_decode_monolithic(
+        self, layer: torch.nn.Module, x: torch.Tensor
+    ) -> bool:
+        use_softmax_logits_path = (
+            self._llm_scaler_moe_logits_requested()
+            and getattr(layer, "top_k", None) == 2
+            and getattr(layer, "renormalize", None) is True
+        )
+        use_minimax_logits_path = (
+            self._llm_scaler_moe_minimax_logits_requested()
+            and getattr(layer, "top_k", None) == 8
+            and getattr(layer, "renormalize", None) is True
+            and getattr(layer, "scoring_func", None) == "sigmoid"
+            and getattr(layer, "e_score_correction_bias", None) is not None
+        )
+        return (
+            (use_softmax_logits_path or use_minimax_logits_path)
+            and getattr(layer, "_llm_scaler_moe_u4_decode", False)
+            and x.dtype in (torch.float16, torch.bfloat16)
+            and x.shape[0] <= 4
+            and getattr(layer, "expert_map", None) is None
+            and not getattr(layer, "use_grouped_topk", False)
+        )
+
+    def _can_use_llm_scaler_moe(self, layer: torch.nn.Module) -> bool:
+        expert_map = getattr(layer, "expert_map", None)
+        expert_layout_supported = (
+            (
+                expert_map is None
+                and getattr(layer, "global_num_experts", None)
+                == getattr(layer, "local_num_experts", None)
+            )
+            or (
+                expert_map is not None
+                and getattr(layer, "local_num_experts", 0) > 0
+                and getattr(layer, "global_num_experts", 0)
+                > getattr(layer, "local_num_experts", 0)
+            )
+        )
+        return (
+            self._llm_scaler_moe_requested()
+            and current_platform.is_xpu()
+            and self.quant_config.weight_bits == 4
+            and not self.quant_config.has_zp
+            and expert_layout_supported
+            and not getattr(layer, "apply_router_weight_on_input", False)
+            and getattr(layer, "group_size", None) == 128
+            and getattr(layer, "w13_qweight", None) is not None
+            and getattr(layer, "w2_qweight", None) is not None
+            and layer.w13_qweight.dtype == torch.uint8
+            and layer.w2_qweight.dtype == torch.uint8
+            and layer.w13_scales.dtype in (torch.float16, torch.bfloat16)
+            and layer.w2_scales.dtype in (torch.float16, torch.bfloat16)
+        )
+
+    def process_weights_after_loading(self, layer: torch.nn.Module) -> None:
+        if not self._can_use_llm_scaler_moe(layer):
+            return
+        if getattr(layer, "_llm_scaler_moe_u4_decode", False):
+            return
+
+        layer._llm_scaler_w13_scales = (
+            layer.w13_scales
+            if layer.w13_scales.dtype == torch.float16
+            else layer.w13_scales.to(torch.float16).contiguous()
+        )
+        layer._llm_scaler_w2_scales = (
+            layer.w2_scales
+            if layer.w2_scales.dtype == torch.float16
+            else layer.w2_scales.to(torch.float16).contiguous()
+        )
+        layer._llm_scaler_moe_u4_decode = True
+        logger.info_once(
+            "Enabled llm-scaler XPU INT4 MoE decode path for %s "
+            "(scale_dtype=%s)",
+            layer.layer_name,
+            layer.w13_scales.dtype,
+        )
+
     def apply(
         self,
         layer: FusedMoE,
@@ -376,6 +477,148 @@ class MoeWNA16Method(FusedMoEMethodBase):
             f"Only SiLU activation is supported, not {layer.activation}."
         )
 
+        if (
+            getattr(layer, "_llm_scaler_moe_u4_decode", False)
+            and shared_experts_input is None
+            and x.dtype in (torch.float16, torch.bfloat16)
+            and x.shape[0] <= 4
+        ):
+            try:
+                if self._llm_scaler_moe_ws_requested():
+                    from custom_esimd_kernels_vllm import (
+                        moe_forward_tiny_cutlass_nmajor_int4_u4_ws as moe_forward_tiny_cutlass_nmajor_int4_u4,
+                    )
+                else:
+                    from custom_esimd_kernels_vllm import (
+                        moe_forward_tiny_cutlass_nmajor_int4_u4,
+                    )
+
+                expert_map = getattr(layer, "expert_map", None)
+                if expert_map is not None:
+                    mapped_topk_ids = expert_map[topk_ids.long()]
+                    local_mask = mapped_topk_ids >= 0
+                    mapped_topk_weights = topk_weights * local_mask.to(
+                        topk_weights.dtype
+                    )
+                    mapped_topk_ids = torch.where(
+                        local_mask, mapped_topk_ids, torch.zeros_like(mapped_topk_ids)
+                    )
+                else:
+                    mapped_topk_ids = topk_ids
+                    mapped_topk_weights = topk_weights
+                return moe_forward_tiny_cutlass_nmajor_int4_u4(
+                    x,
+                    layer.w13_qweight,
+                    layer._llm_scaler_w13_scales,
+                    layer.w2_qweight,
+                    layer._llm_scaler_w2_scales,
+                    mapped_topk_weights.contiguous(),
+                    mapped_topk_ids.contiguous(),
+                )
+            except Exception:
+                logger.exception("llm-scaler XPU INT4 MoE path failed")
+                raise
+
+        return fused_experts(
+            x,
+            layer.w13_qweight,
+            layer.w2_qweight,
+            topk_weights=topk_weights,
+            topk_ids=topk_ids,
+            inplace=not self.moe.disable_inplace,
+            apply_router_weight_on_input=layer.apply_router_weight_on_input,
+            global_num_experts=layer.global_num_experts,
+            expert_map=layer.expert_map,
+            quant_config=self.moe_quant_config,
+        )
+
+    def apply_monolithic(
+        self,
+        layer: FusedMoE,
+        x: torch.Tensor,
+        router_logits: torch.Tensor,
+        input_ids: torch.Tensor | None = None,
+    ) -> torch.Tensor:
+        if (
+            self._llm_scaler_moe_minimax_logits_requested()
+            and getattr(layer, "_llm_scaler_moe_u4_decode", False)
+            and x.dtype in (torch.float16, torch.bfloat16)
+            and x.shape[0] <= 4
+            and getattr(layer, "top_k", None) == 8
+            and getattr(layer, "renormalize", None) is True
+            and getattr(layer, "scoring_func", None) == "sigmoid"
+            and getattr(layer, "e_score_correction_bias", None) is not None
+            and getattr(layer, "expert_map", None) is None
+            and not getattr(layer, "use_grouped_topk", False)
+        ):
+            try:
+                from custom_esimd_kernels_vllm import (
+                    moe_forward_tiny_cutlass_nmajor_int4_u4_minimax,
+                )
+
+                logger.info_once(
+                    "Using llm-scaler XPU INT4 MiniMax logits decode path"
+                )
+                return moe_forward_tiny_cutlass_nmajor_int4_u4_minimax(
+                    x.contiguous(),
+                    layer.w13_qweight,
+                    layer._llm_scaler_w13_scales,
+                    layer.w2_qweight,
+                    layer._llm_scaler_w2_scales,
+                    router_logits.contiguous(),
+                    layer.e_score_correction_bias,
+                    int(layer.top_k),
+                    bool(layer.renormalize),
+                )
+            except Exception:
+                logger.exception("llm-scaler XPU INT4 MiniMax logits path failed")
+                raise
+
+        if (
+            self._llm_scaler_moe_logits_requested()
+            and getattr(layer, "_llm_scaler_moe_u4_decode", False)
+            and x.dtype in (torch.float16, torch.bfloat16)
+            and x.shape[0] <= 4
+            and getattr(layer, "top_k", None) == 2
+            and getattr(layer, "renormalize", None) is True
+            and getattr(layer, "expert_map", None) is None
+            and not getattr(layer, "use_grouped_topk", False)
+        ):
+            try:
+                from custom_esimd_kernels_vllm import (
+                    moe_forward_tiny_cutlass_nmajor_int4_u4_logits,
+                )
+
+                logger.info_once(
+                    "Using llm-scaler XPU INT4 MoE logits decode path"
+                )
+                x = x.contiguous()
+                router_logits = router_logits.contiguous()
+                return moe_forward_tiny_cutlass_nmajor_int4_u4_logits(
+                    x,
+                    layer.w13_qweight,
+                    layer._llm_scaler_w13_scales,
+                    layer.w2_qweight,
+                    layer._llm_scaler_w2_scales,
+                    router_logits,
+                )
+            except Exception:
+                logger.exception("llm-scaler XPU INT4 MoE logits path failed")
+                raise
+
+        from vllm.model_executor.layers.fused_moe import fused_experts
+        from vllm.model_executor.layers.fused_moe.router.fused_topk_router import (
+            fused_topk,
+        )
+
+        topk_weights, topk_ids, _ = fused_topk(
+            hidden_states=x,
+            gating_output=router_logits,
+            topk=layer.top_k,
+            renormalize=layer.renormalize,
+            indices_type=self.topk_indices_dtype,
+        )
+
         return fused_experts(
             x,
             layer.w13_qweight,
diff --git a/vllm/model_executor/models/minimax_m2.py b/vllm/model_executor/models/minimax_m2.py
index 818b7e681..1d2769ba3 100644
--- a/vllm/model_executor/models/minimax_m2.py
+++ b/vllm/model_executor/models/minimax_m2.py
@@ -23,6 +23,7 @@
 # limitations under the License.
 """Inference-only MiniMaxM2 model."""
 
+import os
 from collections.abc import Iterable
 from itertools import islice
 from typing import Any
@@ -33,6 +34,7 @@ from transformers import PretrainedConfig
 
 from vllm.compilation.decorators import support_torch_compile
 from vllm.config import CacheConfig, ModelConfig, VllmConfig
+from vllm.distributed.communication_op import tensor_model_parallel_all_reduce
 from vllm.distributed import (
     get_pp_group,
     get_tensor_model_parallel_rank,
@@ -62,6 +64,7 @@ from vllm.model_executor.model_loader.weight_utils import (
     maybe_remap_kv_scale_name,
 )
 from vllm.sequence import IntermediateTensors
+from vllm.utils.xpu_decode_timing import timed_region
 
 from .interfaces import EagleModelMixin, SupportsEagle3, SupportsLoRA, SupportsPP
 from .utils import (
@@ -74,6 +77,86 @@ from .utils import (
 )
 
 
+_MINIMAX_M2_FP16_ROUTER = (
+    os.environ.get("VLLM_MINIMAX_M2_FP16_ROUTER", "0") == "1"
+)
+_MINIMAX_M2_FP16_ROUTER_AUDIT = (
+    os.environ.get("VLLM_MINIMAX_M2_FP16_ROUTER_AUDIT", "0") == "1"
+)
+_MINIMAX_M2_CANDIDATE_ROUTER_TOPM = int(
+    os.environ.get("VLLM_MINIMAX_M2_CANDIDATE_ROUTER_TOPM", "0")
+)
+_MINIMAX_M2_CANDIDATE_ROUTER_MAX_TOKENS = int(
+    os.environ.get("VLLM_MINIMAX_M2_CANDIDATE_ROUTER_MAX_TOKENS", "4")
+)
+_MINIMAX_M2_CANDIDATE_ROUTER_XPU_REPAIR = (
+    os.environ.get("VLLM_MINIMAX_M2_CANDIDATE_ROUTER_XPU_REPAIR", "0") == "1"
+)
+_MINIMAX_M2_ATTN_DELAY_ALLREDUCE = (
+    os.environ.get("VLLM_MINIMAX_M2_ATTN_DELAY_ALLREDUCE", "0") == "1"
+)
+_MINIMAX_M2_MOE_DELAY_ALLREDUCE = (
+    os.environ.get("VLLM_MINIMAX_MOE_DELAY_ALLREDUCE", "0") == "1"
+)
+_MINIMAX_QK_RMS_XPU_HELPER = (
+    os.environ.get("VLLM_MINIMAX_QK_RMS_XPU_HELPER", "0") == "1"
+)
+_MINIMAX_QK_RMS_XPU_HELPER_MAX_TOKENS = int(
+    os.environ.get("VLLM_MINIMAX_QK_RMS_XPU_HELPER_MAX_TOKENS", "4")
+)
+_minimax_qk_rms_xpu = None
+if _MINIMAX_QK_RMS_XPU_HELPER:
+    try:
+        import minimax_qk_rms_xpu as _minimax_qk_rms_xpu
+
+        try:
+            torch.library.register_fake("minimax_qk_rms_xpu::var")(
+                lambda qkv, qk_var, q_size, kv_size: None
+            )
+            torch.library.register_fake("minimax_qk_rms_xpu::apply")(
+                lambda qkv,
+                qk_var,
+                q_weight,
+                k_weight,
+                q_out,
+                k_out,
+                q_size,
+                kv_size,
+                eps: None
+            )
+        except RuntimeError:
+            pass
+    except Exception:
+        _minimax_qk_rms_xpu = None
+
+_minimax_m2_candidate_repair_topk = None
+_minimax_m2_candidate_repair_import_failed = False
+
+
+def _get_minimax_m2_candidate_repair_topk():
+    global _minimax_m2_candidate_repair_topk
+    global _minimax_m2_candidate_repair_import_failed
+    if (
+        not _MINIMAX_M2_CANDIDATE_ROUTER_XPU_REPAIR
+        or _minimax_m2_candidate_repair_import_failed
+    ):
+        return None
+    if _minimax_m2_candidate_repair_topk is not None:
+        return _minimax_m2_candidate_repair_topk
+    try:
+        from custom_esimd_kernels_vllm import (
+            minimax_m2_candidate_repair_topk as repair_topk,
+        )
+        if not hasattr(torch.ops.moe_int4_ops, "minimax_m2_candidate_repair_topk"):
+            _minimax_m2_candidate_repair_import_failed = True
+            return None
+    except Exception:
+        _minimax_m2_candidate_repair_import_failed = True
+        return None
+    _minimax_m2_candidate_repair_topk = repair_topk
+    return repair_topk
+
+
 class MiniMaxM2MoE(nn.Module):
     def __init__(
         self,
@@ -83,6 +166,8 @@ class MiniMaxM2MoE(nn.Module):
     ):
         super().__init__()
         self.tp_size = get_tensor_model_parallel_world_size()
+        self.layer_name = prefix
+        self._fp16_router_audit_printed: set[str] = set()
 
         if self.tp_size > config.num_local_experts:
             raise ValueError(
@@ -127,15 +212,210 @@ class MiniMaxM2MoE(nn.Module):
         assert param.size() == loaded_weight.size()
         param.data.copy_(loaded_weight.to(torch.float32))
 
+    def _audit_fp16_router(
+        self,
+        router_logits_fp32: torch.Tensor,
+        router_logits_fp16: torch.Tensor,
+        num_tokens: int,
+    ) -> None:
+        audit_key = "decode" if num_tokens == 1 else "prefill"
+        if audit_key in self._fp16_router_audit_printed:
+            return
+        self._fp16_router_audit_printed.add(audit_key)
+
+        top_k = int(getattr(self.experts, "top_k", 8))
+        with torch.no_grad():
+            fp32_choice_scores = self._router_choice_scores(router_logits_fp32)
+            fp16_choice_scores = self._router_choice_scores(router_logits_fp16)
+            _, fp32_idx = torch.topk(fp32_choice_scores, k=top_k, dim=-1)
+            _, fp16_idx = torch.topk(fp16_choice_scores, k=top_k, dim=-1)
+            ordered_mismatch = (fp32_idx != fp16_idx).any(dim=-1).sum().item()
+            set_mismatch = (
+                torch.sort(fp32_idx, dim=-1).values
+                != torch.sort(fp16_idx, dim=-1).values
+            ).any(dim=-1).sum().item()
+            max_abs_logit_delta = (
+                router_logits_fp32 - router_logits_fp16
+            ).abs().max().item()
+            candidate_misses: list[tuple[int, int]] = []
+            max_candidates = router_logits_fp16.shape[-1]
+            for candidate_count in (12, 16, 32):
+                if candidate_count > max_candidates:
+                    continue
+                _, fp16_candidates = torch.topk(
+                    fp16_choice_scores, k=candidate_count, dim=-1
+                )
+                covered = (
+                    fp32_idx.unsqueeze(-1) == fp16_candidates.unsqueeze(-2)
+                ).any(dim=-1).all(dim=-1)
+                candidate_misses.append(
+                    (candidate_count, (~covered).sum().item())
+                )
+            max_abs_choice_delta = (
+                fp32_choice_scores - fp16_choice_scores
+            ).abs().max().item()
+
+        print(
+            "[minimax-fp16-router-audit] "
+            f"layer={self.layer_name} phase={audit_key} tokens={num_tokens} "
+            f"top_k={top_k} ordered_mismatch={ordered_mismatch} "
+            f"set_mismatch={set_mismatch} "
+            f"max_abs_logit_delta={max_abs_logit_delta:.6g} "
+            f"max_abs_choice_delta={max_abs_choice_delta:.6g} "
+            + " ".join(
+                f"top{candidate_count}_candidate_miss={miss_count}"
+                for candidate_count, miss_count in candidate_misses
+            ),
+            flush=True,
+        )
+
+    def _router_choice_scores(self, router_logits: torch.Tensor) -> torch.Tensor:
+        scoring_func = getattr(self.experts, "scoring_func", "softmax")
+        if scoring_func == "sigmoid":
+            scores = router_logits.sigmoid()
+        elif scoring_func == "softmax":
+            scores = router_logits.softmax(dim=-1)
+        elif scoring_func == "sqrtsoftplus":
+            scores = torch.nn.functional.softplus(router_logits).sqrt()
+        else:
+            scores = router_logits
+        if self.e_score_correction_bias is not None:
+            scores = scores + self.e_score_correction_bias.to(
+                scores.dtype
+            ).unsqueeze(0)
+        return scores
+
+    def _candidate_router_topk(
+        self, hidden_states: torch.Tensor, candidate_count: int
+    ) -> tuple[torch.Tensor, torch.Tensor, torch.Tensor]:
+        top_k = int(getattr(self.experts, "top_k", 8))
+        candidate_count = max(top_k, candidate_count)
+        gate_weight_fp32 = getattr(self.gate, "weight", None)
+        if gate_weight_fp32 is None:
+            raise RuntimeError("MiniMax candidate router requires gate.weight")
+        if getattr(self.experts, "scoring_func", "softmax") != "sigmoid":
+            raise RuntimeError("MiniMax candidate router currently expects sigmoid routing")
+        if self.e_score_correction_bias is None:
+            raise RuntimeError("MiniMax candidate router expects correction bias")
+
+        router_logits_fp16 = torch.nn.functional.linear(
+            hidden_states.to(torch.float16), self._gate_weight_fp16
+        ).to(torch.float32)
+        choice_scores_fp16 = self._router_choice_scores(router_logits_fp16)
+        _, candidate_ids = torch.topk(
+            choice_scores_fp16, k=candidate_count, dim=-1
+        )
+
+        repair_topk = _get_minimax_m2_candidate_repair_topk()
+        if repair_topk is not None and hidden_states.device.type == "xpu":
+            topk_weights, topk_ids = repair_topk(
+                hidden_states,
+                candidate_ids,
+                gate_weight_fp32,
+                self.e_score_correction_bias,
+                top_k,
+                getattr(self.experts, "renormalize", True),
+            )
+            return topk_weights.contiguous(), topk_ids.contiguous(), router_logits_fp16
+
+        candidate_weights = gate_weight_fp32[candidate_ids.long()]
+        exact_logits = (
+            candidate_weights * hidden_states.to(torch.float32).unsqueeze(1)
+        ).sum(dim=-1)
+        exact_scores = exact_logits.sigmoid()
+        exact_choice_scores = exact_scores + self.e_score_correction_bias[
+            candidate_ids.long()
+        ].to(exact_scores.dtype)
+        _, local_topk = torch.topk(exact_choice_scores, k=top_k, dim=-1)
+        topk_ids = candidate_ids.gather(1, local_topk).to(torch.int32)
+        topk_weights = exact_scores.gather(1, local_topk).to(torch.float32)
+        if getattr(self.experts, "renormalize", True):
+            topk_weights = topk_weights / topk_weights.sum(dim=-1, keepdim=True)
+        return topk_weights.contiguous(), topk_ids.contiguous(), router_logits_fp16
+
+    def _can_use_candidate_router(
+        self, hidden_states: torch.Tensor, num_tokens: int
+    ) -> bool:
+        if (
+            _MINIMAX_M2_CANDIDATE_ROUTER_TOPM <= 0
+            or not hasattr(self, "_gate_weight_fp16")
+            or num_tokens > _MINIMAX_M2_CANDIDATE_ROUTER_MAX_TOKENS
+        ):
+            return False
+        runner = self.experts.runner
+        if getattr(runner, "do_naive_dispatch_combine", False):
+            return False
+        if getattr(runner.moe_config, "pcp_size", 1) != 1:
+            return False
+        quant_method = getattr(runner, "_quant_method", None)
+        if quant_method is None or getattr(quant_method, "is_monolithic", False):
+            return False
+        should_apply_decode_monolithic = getattr(
+            quant_method, "should_apply_decode_monolithic", None
+        )
+        if (
+            should_apply_decode_monolithic is not None
+            and should_apply_decode_monolithic(self.experts, hidden_states)
+        ):
+            return False
+        return True
+
     def forward(self, hidden_states: torch.Tensor) -> torch.Tensor:
         num_tokens, hidden_dim = hidden_states.shape
         hidden_states = hidden_states.view(-1, hidden_dim)
 
+        if self._can_use_candidate_router(hidden_states, num_tokens):
+            with timed_region("minimax.moe.router_linear"):
+                topk_weights, topk_ids, router_logits = self._candidate_router_topk(
+                    hidden_states, _MINIMAX_M2_CANDIDATE_ROUTER_TOPM
+                )
+                if _MINIMAX_M2_FP16_ROUTER_AUDIT:
+                    router_logits_fp32, _ = self.gate(hidden_states.to(torch.float32))
+                    self._audit_fp16_router(
+                        router_logits_fp32, router_logits, num_tokens
+                    )
+                self.experts.router._precomputed_topk = (topk_weights, topk_ids)
+            with timed_region("minimax.moe.experts_total"):
+                try:
+                    final_hidden_states = self.experts(
+                        hidden_states=hidden_states, router_logits=router_logits
+                    )
+                finally:
+                    if (
+                        getattr(self.experts.router, "_precomputed_topk", None)
+                        is not None
+                    ):
+                        self.experts.router._precomputed_topk = None
+            return final_hidden_states.view(num_tokens, hidden_dim)
+
         # router_logits: (num_tokens, n_experts)
-        router_logits, _ = self.gate(hidden_states.to(torch.float32))
-        final_hidden_states = self.experts(
-            hidden_states=hidden_states, router_logits=router_logits
-        )
+        with timed_region("minimax.moe.router_linear"):
+            if _MINIMAX_M2_FP16_ROUTER and hasattr(self, "_gate_weight_fp16"):
+                router_logits_fp16 = torch.nn.functional.linear(
+                    hidden_states.to(torch.float16), self._gate_weight_fp16
+                ).to(torch.float32)
+                if _MINIMAX_M2_FP16_ROUTER_AUDIT:
+                    router_logits_fp32, _ = self.gate(hidden_states.to(torch.float32))
+                    self._audit_fp16_router(
+                        router_logits_fp32, router_logits_fp16, num_tokens
+                    )
+                router_logits = router_logits_fp16
+            else:
+                router_logits, _ = self.gate(hidden_states.to(torch.float32))
+                if (
+                    _MINIMAX_M2_FP16_ROUTER_AUDIT
+                    and hasattr(self, "_gate_weight_fp16")
+                ):
+                    router_logits_fp16 = torch.nn.functional.linear(
+                        hidden_states.to(torch.float16), self._gate_weight_fp16
+                    ).to(torch.float32)
+                    self._audit_fp16_router(
+                        router_logits, router_logits_fp16, num_tokens
+                    )
+        with timed_region("minimax.moe.experts_total"):
+            final_hidden_states = self.experts(
+                hidden_states=hidden_states, router_logits=router_logits
+            )
 
         return final_hidden_states.view(num_tokens, hidden_dim)
 
@@ -195,6 +475,7 @@ class MiniMaxM2Attention(nn.Module):
             bias=False,
             quant_config=quant_config,
             prefix=f"{prefix}.o_proj",
+            reduce_results=not _MINIMAX_M2_ATTN_DELAY_ALLREDUCE,
         )
 
         if (
@@ -221,33 +502,71 @@ class MiniMaxM2Attention(nn.Module):
         self.q_norm = MiniMaxText01RMSNormTP(
             self.head_dim * self.total_num_heads, eps=rms_norm_eps
         )
-        if self.total_num_kv_heads >= tp_size:
-            self.k_norm = MiniMaxText01RMSNormTP(
-                self.head_dim * self.total_num_kv_heads, eps=rms_norm_eps
-            )
-        else:
-            # KV heads are replicated across TP ranks; shard k_norm weight by
-            # total_num_kv_heads rather than tp_size to avoid incorrect sharding.
-            num_kv_head_replicas = tp_size // self.total_num_kv_heads
-            self.k_norm = MiniMaxText01RMSNormTP(
-                self.head_dim * self.total_num_kv_heads,
-                eps=rms_norm_eps,
-                weight_shard_world_size=self.total_num_kv_heads,
-                weight_shard_rank=get_tensor_model_parallel_rank()
-                // num_kv_head_replicas,
-            )
+        self.k_norm = MiniMaxText01RMSNormTP(
+            self.head_dim * self.total_num_kv_heads, eps=rms_norm_eps
+        )
 
     def forward(
         self,
         positions: torch.Tensor,
         hidden_states: torch.Tensor,
     ) -> torch.Tensor:
-        qkv, _ = self.qkv_proj(hidden_states)
-        q, k, v = qkv.split([self.q_size, self.kv_size, self.kv_size], dim=-1)
-        q, k = MiniMaxText01RMSNormTP.forward_qk(self.q_norm, self.k_norm, q, k)
-        q, k = self.rotary_emb(positions, q, k)
-        attn_output = self.attn(q, k, v)
-        output, _ = self.o_proj(attn_output)
+        with timed_region("minimax.attn.qkv"):
+            qkv, _ = self.qkv_proj(hidden_states)
+        use_qk_helper = (
+            _MINIMAX_QK_RMS_XPU_HELPER
+            and qkv.shape[0] <= _MINIMAX_QK_RMS_XPU_HELPER_MAX_TOKENS
+            and qkv.is_xpu
+            and qkv.is_contiguous()
+            and qkv.dtype in (torch.float16, torch.bfloat16)
+            and self.q_norm.weight.dtype == qkv.dtype
+            and self.k_norm.weight.dtype == qkv.dtype
+            and _minimax_qk_rms_xpu is not None
+            and hasattr(torch.ops, "minimax_qk_rms_xpu")
+        )
+        if use_qk_helper:
+            with timed_region("minimax.attn.qk_norm"):
+                qk_var = torch.empty(
+                    (qkv.shape[0], 2), device=qkv.device, dtype=torch.float32
+                )
+                torch.ops.minimax_qk_rms_xpu.var(
+                    qkv, qk_var, self.q_size, self.kv_size
+                )
+                if self.q_norm.tp_world > 1:
+                    qk_var = (
+                        tensor_model_parallel_all_reduce(qk_var)
+                        / self.q_norm.tp_world
+                    )
+                q = torch.empty(
+                    (qkv.shape[0], self.q_size), device=qkv.device, dtype=qkv.dtype
+                )
+                k = torch.empty(
+                    (qkv.shape[0], self.kv_size), device=qkv.device, dtype=qkv.dtype
+                )
+                torch.ops.minimax_qk_rms_xpu.apply(
+                    qkv,
+                    qk_var,
+                    self.q_norm.weight,
+                    self.k_norm.weight,
+                    q,
+                    k,
+                    self.q_size,
+                    self.kv_size,
+                    self.q_norm.variance_epsilon,
+                )
+            v = qkv.split([self.q_size, self.kv_size, self.kv_size], dim=-1)[2]
+        else:
+            q, k, v = qkv.split([self.q_size, self.kv_size, self.kv_size], dim=-1)
+            with timed_region("minimax.attn.qk_norm"):
+                q, k = MiniMaxText01RMSNormTP.forward_qk(
+                    self.q_norm, self.k_norm, q, k
+                )
+        with timed_region("minimax.attn.rope"):
+            q, k = self.rotary_emb(positions, q, k)
+        with timed_region("minimax.attn.kv_attention"):
+            attn_output = self.attn(q, k, v)
+        with timed_region("minimax.attn.o_proj"):
+            output, _ = self.o_proj(attn_output)
         return output
 
 
@@ -292,11 +611,20 @@ class MiniMaxM2DecoderLayer(nn.Module):
             quant_config=quant_config,
             prefix=f"{prefix}.mlp",
         )
+        self.tp_rank = get_tensor_model_parallel_rank()
+        self.tp_size = get_tensor_model_parallel_world_size()
         self.input_layernorm = RMSNorm(config.hidden_size, eps=config.rms_norm_eps)
         self.post_attention_layernorm = RMSNorm(
             config.hidden_size, eps=config.rms_norm_eps
         )
 
+    def _allreduce_with_rank0_residual(
+        self, hidden_states: torch.Tensor, residual: torch.Tensor
+    ) -> torch.Tensor:
+        if self.tp_rank == 0:
+            hidden_states = hidden_states + residual
+        return tensor_model_parallel_all_reduce(hidden_states)
+
     def forward(
         self,
         positions: torch.Tensor,
@@ -307,6 +635,13 @@ class MiniMaxM2DecoderLayer(nn.Module):
         if residual is None:
             residual = hidden_states
             hidden_states = self.input_layernorm(hidden_states)
+        elif _MINIMAX_M2_MOE_DELAY_ALLREDUCE and self.tp_size > 1:
+            with timed_region("minimax.moe.delayed_residual_allreduce"):
+                hidden_states = self._allreduce_with_rank0_residual(
+                    hidden_states, residual
+                )
+            residual = hidden_states
+            hidden_states = self.input_layernorm(hidden_states)
         else:
             hidden_states, residual = self.input_layernorm(hidden_states, residual)
         hidden_states = self.self_attn(
@@ -315,7 +650,17 @@ class MiniMaxM2DecoderLayer(nn.Module):
         )
 
         # Fully Connected
-        hidden_states, residual = self.post_attention_layernorm(hidden_states, residual)
+        if _MINIMAX_M2_ATTN_DELAY_ALLREDUCE and self.tp_size > 1:
+            with timed_region("minimax.attn.delayed_residual_allreduce"):
+                hidden_states = self._allreduce_with_rank0_residual(
+                    hidden_states, residual
+                )
+            residual = hidden_states
+            hidden_states = self.post_attention_layernorm(hidden_states)
+        else:
+            hidden_states, residual = self.post_attention_layernorm(
+                hidden_states, residual
+            )
 
         hidden_states = self.block_sparse_moe(hidden_states)
 
@@ -334,6 +679,8 @@ class MiniMaxM2Model(nn.Module, EagleModelMixin):
         cache_config = vllm_config.cache_config
         quant_config = vllm_config.quant_config
         self.config = config
+        self.tp_rank = get_tensor_model_parallel_rank()
+        self.tp_size = get_tensor_model_parallel_world_size()
 
         self.vocab_size = config.vocab_size
 
@@ -401,7 +748,17 @@ class MiniMaxM2Model(nn.Module, EagleModelMixin):
             return IntermediateTensors(
                 {"hidden_states": hidden_states, "residual": residual}
             )
-        hidden_states, _ = self.norm(hidden_states, residual)
+        if (
+            _MINIMAX_M2_MOE_DELAY_ALLREDUCE
+            and self.tp_size > 1
+            and residual is not None
+        ):
+            if self.tp_rank == 0:
+                hidden_states = hidden_states + residual
+            hidden_states = tensor_model_parallel_all_reduce(hidden_states)
+            hidden_states = self.norm(hidden_states)
+        else:
+            hidden_states, _ = self.norm(hidden_states, residual)
 
         if len(aux_hidden_states) > 0:
             return hidden_states, aux_hidden_states
@@ -417,6 +774,22 @@ class MiniMaxM2Model(nn.Module, EagleModelMixin):
             num_experts=self.config.num_local_experts,
         )
 
+    def _init_fp16_router_buffers(self) -> None:
+        for layer in self.layers:
+            if not isinstance(layer, MiniMaxM2DecoderLayer):
+                continue
+            moe = layer.block_sparse_moe
+            gate_weight = getattr(moe.gate, "weight", None)
+            if gate_weight is None:
+                continue
+            gate_weight_fp16 = gate_weight.detach().to(torch.float16).contiguous()
+            if "_gate_weight_fp16" in moe._buffers:
+                moe._buffers["_gate_weight_fp16"] = gate_weight_fp16
+            else:
+                moe.register_buffer(
+                    "_gate_weight_fp16", gate_weight_fp16, persistent=False
+                )
+
     def load_weights(self, weights: Iterable[tuple[str, torch.Tensor]]) -> set[str]:
         stacked_params_mapping = [
             # (param_name, shard_name, shard_id)
@@ -513,6 +886,12 @@ class MiniMaxM2Model(nn.Module, EagleModelMixin):
                     )
                     weight_loader(param, loaded_weight)
             loaded_params.add(name)
+        if (
+            _MINIMAX_M2_FP16_ROUTER
+            or _MINIMAX_M2_FP16_ROUTER_AUDIT
+            or _MINIMAX_M2_CANDIDATE_ROUTER_TOPM > 0
+        ):
+            self._init_fp16_router_buffers()
         return loaded_params
 
 
